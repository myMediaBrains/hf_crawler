# 로컬 AI 에이전트 플랫폼 구축 가이드

> 작성일: 2026-08-04 22:23 (KST)
> 최종 수정: 2026-08-06 05:44 (KST)
> 환경: FastAPI 백엔드 + Vite React 프론트엔드 + Ollama/Qwen (M1 Max 32GB, 내장 2TB SSD / 외장 SSD는 추후 도입 옵션)

> **전체 전략 로드맵 (3단계)**
> | 단계 | 환경 | 목적 | 비용 태도 |
> |---|---|---|---|
> | 1단계 (현재) | 맥북 온디맨드 | 다양한 AI 서비스 폭넓게 실험 (17장 카탈로그) | 최소 비용, 로컬 완결 |
> | 2단계 (과도기) | 홈 미니서버 상시운영 (19장) | 수익성 검증 전, 자동화를 실제 상시로 돌려보며 검증 | 하드웨어 투자만, 수익 불확실 상태의 베팅 |
> | 3단계 (수익화 확정 시) | 외부 클라우드 인프라 | 검증된 수익모델을 안정적으로 상용 운영 | 수익으로 정당화되는 비용 지출 |
> 1→2단계 전환 트리거: 실험한 스킬 중 "상시 자동화할 가치가 있다"는 판단이 설 때. 2→3단계 전환 트리거: 수익이 실제 발생하거나 명확히 예측 가능해질 때(미니서버의 동시성·신뢰성·단일장애점 한계가 실제 걸림돌이 되는 시점).
> 전자상거래(구매대행·쇼핑몰)는 어느 단계에서도 지금 설계의 자연스러운 확장이 아닌 별도 프로젝트로 분리해서 검토.

> **운영 전제 (1단계 기준)**
> - 개인 전용 (멀티유저 아님) — 인증/권한 체계 최소화
> - **데이터는 우선 내장 2TB SSD에 저장** (외장 SSD 구매 계획 없음 — 필요해지면 추후 도입, 15장 참고)
> - 1단계는 상용 서비스화가 목표 아님 (실험/학습 목적) — 상용화는 3단계에서 별도 인프라로 추진
> - **맥북 온디맨드 운영 확정 (1단계)** — 상시 서비스는 비용·보안·1인 운영 부담을 이유로 보류, 이용할 때만 가동 (근거는 19장 참고)
> - 백그라운드 자동화·수집도 맥북이 켜져 있을 때만
> - 자동 업데이트 없음 — Claude(API/Code)는 **필요시 수동 호출**로만 사용 (비용 예측 가능하게)
> - 미니서버 상시운영은 2단계 과도기 옵션으로 확정 (19장) — 단, 1단계 완료·전환 판단 전까지는 착수하지 않음

---

## 1. 현재 스택 평가

- **FastAPI + Vite React + Ollama(Qwen) 조합은 적합**. 다만 32GB 유니파이드 메모리가 "여러 무거운 에이전트 동시 실행"의 한계점.
- 32GB 기준 추천 모델
  - 코딩: Qwen3-Coder 30B-A3B (~24GB, Q4_K_M)
  - 비전(이미지/문서 분석): Qwen3-VL 30B-A3B
  - 경량 라우팅/빠른 응답: Qwen3.5 9B (~7GB)
- MLX가 Ollama보다 순수 추론 속도가 빠름 (같은 조건에서 2~3배 차이 보고됨). 도구 호출(function calling)이 필요한 곳은 Ollama, 속도가 중요한 단순 채팅은 MLX 병행 고려.

---

## 2. 32GB 메모리 제약 극복 전략

- **모델 상주 최소화**: `OLLAMA_MAX_LOADED_MODELS=1`, 짧은 `keep_alive` 설정으로 안 쓰는 모델 자동 언로드
- **경량 라우터 + 온디맨드 전문 모델** 구조: 9B급 모델이 상시 대기하며 작업을 분류 → 필요할 때만 30B급 모델 로드
- **작업 큐로 직렬화** (Redis + RQ 등): 워커 1개로 무거운 작업을 순차 처리해 메모리 스파이크 방지
- 컨텍스트는 통째로 넣지 말고 청크 단위 RAG로 관리, `num_ctx`도 작업별로 조정 (요약용 8K, 코딩 리뷰 32K 등)

---

## 3. 자체 구축 vs 오픈소스 도입 판단

| 레이어 | 방향 | 이유 |
|---|---|---|
| 핵심 에이전트 로직/프롬프트 전략 | 직접 구축 | 서비스의 차별화 지점 |
| API 서버 뼈대 (FastAPI) | 직접 구축 | 비즈니스 로직과 얽혀 있음 |
| LLM 서빙 | 오픈소스 (Ollama/MLX) | 이미 성숙, 재구현 불필요 |
| 벡터DB/RAG 인프라 | 오픈소스 (Chroma, LanceDB) | 검색 알고리즘 직접 구현은 비효율 |
| 작업 큐/스케줄링 | 오픈소스 (Redis+RQ, Celery) | 엣지케이스 많아 직접 구현 시 버그 위험 |
| SNS 메시징 연동 | 오픈소스 래퍼(OpenClaw 등)를 격리된 서브모듈로 | 외부 API 스펙이 자주 변경됨 |
| 인증/사용자 관리 | 오픈소스 | 보안 검증된 라이브러리가 안전 |

**핵심 원칙: "인프라는 빌리고, 로직은 짓는다."**

판단 순서:
1. 이미 오픈소스로 안정적으로 존재하는가?
2. 내 아키텍처에 깔끔히 붙일 수 있는가?
3. 붙이기 어렵거나 메모리 예산을 초과하면 → 핵심만 참고해 직접 경량 구현
4. 서비스 차별화 포인트가 될 가능성이 있으면 → 처음부터 직접 구축

---

## 4. OpenClaw 평가

- 오픈소스, 모델 애그노스틱 에이전트 프레임워크. WhatsApp, Telegram, Slack, Discord 등 12개 이상 메시징 플랫폼과 이미 연동됨.
- **장점**: SNS/메시지 송출 부분을 직접 구현하지 않아도 됨. Ollama 등 로컬 모델도 연결 가능.
- **단점**: 보안 사고 이력(파일 삭제 사고 등) 존재, Node.js 기반 Gateway 프로세스가 상시 상주해 32GB 환경에 리소스 부담, 프레임워크 자체가 빠르게 변화 중(아직 초기 단계).
- **결론**: 전체 플랫폼으로 도입하기보다, SNS 메시지 송출 기능만 분리해 필요할 때만 기동하는 서브모듈로 활용. 코딩/문서분석/외국어학습 에이전트는 기존 FastAPI 커스텀 구조 유지.
- **온디맨드 운영 제약**: OpenClaw의 heartbeat 스케줄러(주기적 자동 실행)는 맥북이 꺼져 있으면 작동하지 않음. 시간 지정 자동 포스팅 등은 그 시간에 맥북이 켜져 있어야만 실행됨 — 이 제약을 받아들이거나 시간 민감 자동화는 포기.

---

## 5. Skills 개념과 활용

### 개념
- Skills = **재사용 가능한 절차 지식 폴더** (SKILL.md + 선택적 스크립트/템플릿)
- 점진적 로딩(progressive disclosure): 평소엔 이름/설명만 컨텍스트에 상주, 관련성 있을 때만 전체 지침 로드
- Anthropic이 2025년 10월 공개한 오픈 표준 (agentskills.io)

### 라이브러리와의 차이
| | 일반 라이브러리 | Skills |
|---|---|---|
| 호출 방식 | 코드에서 명시적 import | LLM이 관련성 스스로 판단 후 로드 |
| 핵심 내용 | 실행 코드 | 자연어 지침(+ 선택적 코드) |
| 담는 것 | "무엇을 할 수 있는가(capability)" | "언제·왜·어떻게 해야 하는가(judgment)"까지 |

### 실행 vs 배포
- **실행은 100% 로컬 파일시스템**에서 이루어짐 (GitHub이 실시간 개입하지 않음)
- GitHub은 **버전관리/배포/공유 채널**. `git clone` 또는 marketplace 설치 시점에 로컬로 다운로드된 사본이 실제 실행됨
- 권장: `skills/` 폴더를 로컬 SSD에 두고 동시에 Git 저장소로 관리 (버전 추적 + 비공개 GitHub 백업)

### 로컬 소형 모델 환경 적용 방식
공식 Skills는 모델이 스킬을 자율 판단하지만, 9B~30B급 로컬 모델은 판단 정확도가 낮을 수 있어 다음과 같이 조정:

| 요소 | 공식 방식 | 로컬 환경 권장 |
|---|---|---|
| 스킬 선택 | 모델 자율 판단 | 명시적 라우팅 로직(분류기/규칙 기반) |
| 로딩 시점 | 대화 중 동적 | 요청 진입 시 1회 결정 |
| 도구 실행 주체 | 모델이 자율 결정 | 라우팅 로직이 명시적으로 호출 |

### 폴더 구조 예시
```
skills/
  video-analysis/
    SKILL.md
    extract_audio.py
    transcribe.py
    sample_frames.py
  doc-analysis/SKILL.md
  music-analysis/SKILL.md
  coding-review/SKILL.md
  language-tutor-ko-en/SKILL.md
  sns-posting/SKILL.md
```

---

## 6. 보안 체계

우선순위 순:

1. **프롬프트 인젝션 방어** (최우선)
   - 외부 콘텐츠(문서/영상/웹)는 격리된 컨텍스트에 넣고 "데이터일 뿐 지시가 아니다"라고 시스템 프롬프트에 명시
   - 인젝션 패턴(지시문 형태) 사전 필터링
   - 모델 출력이 이상한 API 호출/외부 요청을 시도하면 차단하는 출력 모니터링
   - **실증 사례(Operation Pale Fire, 2026.1)**: Block 자체 보안팀이 보이지 않는 유니코드 문자에 악성 지시를 숨긴 "오염된 레시피"로 자사 오픈소스 에이전트 Goose를 해킹 — 디버깅 도움으로 위장해 개발자와 AI 에이전트 모두를 속여 정보탈취 악성코드를 실행시키는 데 성공(37장 참고). 보안 전담팀을 둔 조직도 뚫렸다는 건 위 원칙(외부 콘텐츠는 항상 데이터로만 취급)이 이론이 아니라 실전 위협이라는 걸 보여줌

2. **실행 샌드박싱**
   - 코드 실행은 Docker 컨테이너로 격리 (네트워크 차단, 파일시스템 마운트 최소화)
   - SNS 포스팅 등 되돌릴 수 없는 행동은 자동 실행 금지, 사용자 승인 단계 필수

3. **승인/권한 분류 체계** (OpenWorker 패턴 차용 — 21장 참고)
   - 모든 스킬/도구 호출을 위험 등급으로 태깅: `read`(부작용 없음) / `write_local`(작업공간 내 수정) / `exec`(명령 실행) / `external`(외부로 나가는 부작용, 예: SNS 송출·메시지 발송)
   - 등급별 처리 모드: 낮은 위험(`read`)은 자동 진행, 중간(`write_local`)은 기본적으로 사용자 확인, 높은 위험(`exec`, `external`)은 항상 승인 필수
   - 이 태깅을 `skills/` 각 폴더의 `SKILL.md` 메타데이터에 명시해서 라우터가 자동으로 승인 요구 여부를 판단하게 구현

4. **최소 권한 원칙**
   - 에이전트/스킬별로 필요한 권한만 부여
   - API 키는 macOS Keychain 또는 `.env`(파일 권한 600)로 관리, 코드 하드코딩 금지
   - 최소 권한 강제 시 보안 사고율이 크게 낮아진다는 연구 결과 존재 (17% vs 76%)

5. **스킬/도구 공급망 검증 (Bumblebee 도입)**
   - 직접 작성한 스킬만 신뢰, 외부 스킬/MCP 서버는 코드 내용 직접 검증 후 사용
   - 설치 스크립트(`curl | bash` 등)는 실행 전 반드시 내용을 먼저 읽어볼 것 — 바로 파이프로 실행하지 않기
   - **Bumblebee**(Perplexity 오픈소스, Apache 2.0) 도입 — npm/PyPI/Go모듈/MCP 설정/에디터·브라우저 확장까지 스캔하는 읽기전용 공급망 감사 도구. 패키지 매니저를 실행하지 않아 스캔 자체가 악성 postinstall 스크립트를 유발할 위험이 없음
   - baseline(일상 점검)/project(특정 저장소)/deep(사고 대응) 프로필 제공 — 신규 도구 설치 전후로 스캔해 diff 확인하는 루틴 권장

6. **API 서버 표준 보안**
   - 인증(JWT), rate limiting, CORS 제한, 입력 검증(SQL 인젝션, 파일 업로드 검증)
   - 로그에 API 키, 개인정보, SNS 메시지 원문 등 민감정보 미기록

7. **데이터 보안**
   - macOS FileVault로 디스크 암호화
   - 개인정보 포함 데이터는 접근 통제 폴더에 분리 보관
   - `.gitignore`, git-secrets로 백업 시 민감정보 실수 커밋 방지

8. **감사(audit) 체계**
   - 모든 에이전트 행동(스킬 실행, 도구 호출, 외부 요청) 로그화
   - SNS 송출 등 민감 액션은 실행 전/후 로그 분리, 이상 패턴 감지 시 자동 정지

9. **macOS 자체 보안 설정**
   - **FileVault**: 디스크 전체 암호화 (7번과 연결, 아직 안 켰다면 우선 적용)
   - **Gatekeeper 유지**: "확인되지 않은 개발자" 앱을 우회 설정으로 습관적으로 열지 않기
   - **방화벽 활성화**: 시스템 설정 → 네트워크 → 방화벽, 불필요한 수신 연결 차단
   - **네트워크 아웃바운드 모니터링**: Little Snitch 등으로 설치한 앱들의 실제 외부 통신 여부 가시화
   - **TCC 권한 점검**: 개인정보 보호 및 보안 설정에서 각 앱(특히 Voicebox처럼 마이크 접근하는 도구)의 마이크·카메라·전체 디스크 접근 권한을 주기적으로 재검토
   - **파일 격리(Quarantine)**: 외부 다운로드 파일의 macOS Quarantine 속성을 임의로 해제·강제 실행하지 않기

### 온디맨드 시작 시 보안 루틴 (16장과 결합)
```
맥북 켤 때:
  1. bumblebee scan --profile baseline 실행
  2. 직전 스캔과 diff 확인 (신규 취약 패키지 유무)
  3. 이상 있으면 해당 도구 격리/제거 후 재검토
  4. 정상이면 정상 서비스 시작 (16장 on_startup()과 함께 실행)
```

---

## 7. 외부 데이터 전처리 파이프라인

### 공통 단계
```
수집(Ingestion) → 검증/보안(Validation) → 추출(Extraction)
→ 정규화(Normalization) → 청킹(Chunking) → 임베딩(Embedding) → 저장/색인(Storage)
```

### 타입별 처리
- **문서**: 텍스트 레이어 추출(스캔본은 OCR) → 헤더/푸터 노이즈 제거, 표는 구조화 파싱 → 문단/섹션 단위 청킹(overlap 100~200토큰)
- **이미지**: 비전 모델(Qwen3-VL) 캡션 생성 + OCR → 해상도 다운스케일, EXIF는 필요시만 보존 → 캡션 텍스트만 임베딩
- **영상**: 오디오 분리 → Whisper로 자막화(타임스탬프 유지) + 프레임 샘플링(5초 간격 등) → 씬/시간 구간 단위 청킹
- **음악**: 메타데이터(장르/BPM/아티스트) 추출, 신호처리는 librosa 등 전통 도구 활용 (가사 원문 보관은 저작권 주의)

### 적용 원칙
- 전처리는 LLM이 아닌 전통 도구(OCR, Whisper, librosa)가 처리 → 정제된 텍스트/임베딩만 LLM에 전달
- 전처리 파이프라인은 LLM 추론 서버(Ollama)와 별도 워커로 분리해 메모리 경쟁 방지
- 벡터DB: LanceDB(파일 기반, 경량) 또는 Chroma / 메타데이터: SQLite (별도 DB 서버 불필요)
- 영상 > 이미지 > 음악 > 문서 순으로 리소스 소모가 크므로, 영상 전처리는 동시 실행 개수 제한

---

## 8. NotebookLM 스타일 기능 구축

### NotebookLM 핵심 기능
- 소스 기반 QA (업로드 자료에서만 답변, 출처 인용)
- 오디오 오버뷰(팟캐스트 형식 생성), 비디오 오버뷰
- 마인드맵, 플래시카드/퀴즈, 리포트/슬라이드 생성
- 1M 토큰 컨텍스트로 여러 자료 종합 분석

### 참고할 오픈소스 (직접 구현 시 아키텍처 참고용)
| 프로젝트 | 특징 |
|---|---|
| Open Notebook | Docker, 18개+ AI 프로바이더(Ollama 포함), 팟캐스트, REST API, MIT |
| SurfSense | Ollama 네이티브, 하이브리드 검색, 빠른 팟캐스트 생성 |
| InsightsLM | 로컬 전용 버전에서 Ollama+Qwen3+Whisper+CoquiTTS 지원 — 스택 유사도 가장 높음 |

### 플랫폼 적용 매핑
- 소스 기반 QA = 기존 RAG 파이프라인(전처리→청킹→임베딩→검색) 그대로 재사용, 답변 시 근거 청크의 메타데이터(출처/페이지/타임스탬프)를 함께 반환
- 오디오 오버뷰 = 신규 스킬 `skills/audio-overview/` (대화 스크립트 생성 + 로컬 TTS인 Coqui/Kokoro 별도 워커)
- 마인드맵/플래시카드/리포트 = 구조화 출력(JSON/마크다운) 스킬, 시각화는 프론트엔드 담당

### 구현 우선순위 (32GB 환경 기준)
1. 소스 기반 QA + 인용 (기존 파이프라인 재사용률 높음)
2. 요약/리포트/플래시카드 (텍스트 생성만 필요, 부담 적음)
3. 팟캐스트/비디오 오버뷰 (TTS/영상 합성 부담 커서 후순위)

### 기대 효과
- Google NotebookLM 대비: 완전 프라이버시, 사용량 제한 없음, 모델 선택권, 오프라인 가능, 자유로운 커스터마이징
- 플랫폼 전체 관점: 인용/근거 추적 패턴을 다른 에이전트(코딩, 영상분석)에도 표준 검증 메커니즘으로 재사용 가능
- 외국어 학습·코딩·SNS 콘텐츠 에이전트와 파이프라인 직접 연결 가능 (예: 원서 QA+플래시카드, 기술문서 기반 코딩 보조, 요약→SNS 초안)

---

## 9. 하네스(Harness) 개념과 적용

### 개념
- **하네스 = 모델을 감싸서 실제로 작업을 수행하는 에이전트로 만드는 소프트웨어 뼈대**
- 공식: **에이전트 = 모델 + 하네스**. 모델 단독으로는 텍스트 입출력만 가능, 도구 실행·상태 유지·다단계 작업 완료는 하네스가 담당
- 비유: 모델 = 엔진, 하네스 = 자동차의 나머지(바퀴·조향·브레이크·계기판)

### 하네스의 6가지 구성 요소
1. **루프** — 모델 호출 → 도구 실행 → 결과 재입력 → 반복
2. **도구 실행** — 모델의 도구 호출을 실제로 수행
3. **메모리/상태** — 세션이 끊겨도 이어서 작업 가능하게 저장
4. **검증** — 출력/작업 결과가 맞는지 확인하는 피드백 루프
5. **가드레일** — 권한 제한, 실행 범위 통제
6. **컨텍스트 엔지니어링** — 컨텍스트 압축, 상태 오프로드, 하위 작업 분리

### 플랫폼 요소 ↔ 하네스 구성 요소 매핑
| 하네스 구성 요소 | 플랫폼 설계 |
|---|---|
| 루프 | FastAPI → 라우터(9B) → 스킬 판단 → 전문 모델 호출 |
| 도구 실행 | 스킬 폴더 내 스크립트(OCR, Whisper, TTS 등) |
| 메모리/상태 | LanceDB(벡터) + SQLite(메타데이터) |
| 검증 | 인용/출처 추적 구조 (NotebookLM 기능에서 설계) |
| 가드레일 | 샌드박스, 최소 권한, 승인 단계 (6장 보안 체계) |
| 컨텍스트 엔지니어링 | 청킹, RAG로 필요한 부분만 컨텍스트 투입 |

### 왜 중요한가
- 좋은 하네스는 중간급 모델도 프로덕션급으로 만들고, 부실한 하네스는 최상위 모델도 신뢰할 수 없게 만듦
- 32GB 환경(Claude/GPT-5급 모델 불가)에서는 **모델 크기의 한계를 하네스 품질로 보완**하는 것이 핵심 전략

---

## 10. 추가로 정리가 필요한 항목 (TODO)

### 우선순위 높음
- **검증(verification) 구체 설계**: 답변 후 소스 일치 여부를 재검사할지, 규칙 기반 체크만 할지 결정 필요
- **멀티 에이전트 오케스트레이션**: "영상분석→요약→SNS포스팅"처럼 스킬이 연쇄되는 복합 작업의 흐름 제어, 하위 작업 분해, 단계별 검증, 실패 시 재시작 지점 설계

### 우선순위 중간 (서비스 성장 시 채우기)
- **실패 복구/재시도 정책**: 도구 호출 실패·타임아웃 시 재시도 횟수, 포기 기준
- **평가/벤치마크 체계**: 스킬/프롬프트 수정 시 성능 비교용 회귀 테스트 셋
- **모니터링/로깅 대시보드**: 감사 로그를 실제로 확인할 로컬 대시보드
- **외국어 학습 에이전트 구체 설계**: 아직 파이프라인 미설계

### 서비스화 단계에서 필요
- **저작권/법적 이슈**: 음악 가사 원문 보관, 콘텐츠 재가공, SNS 자동 포스팅의 저작물 이용 범위
- **배포/업데이트 전략**: 모델·스킬 버전 교체 시 무중단 전환 방법

---

## 11. 플랫폼 자체 업데이트 — Qwen의 자율성 범위 (수동 트리거 방식)

로컬 Qwen이 플랫폼 코드 자체를 스스로 수정("self-modifying agent")하는 것은 기술적으로 가능하나, **완전 자율은 비권장** — 통제된 반자율 방식으로 설계. 자동 업데이트는 비용이 발생하고 맥북이 상시 켜져 있지 않으므로 **스케줄러 대신 수동 트리거로만 동작**.

### 위험 요인
- 30B급 모델은 복잡한 아키텍처 변경에서 판단력 한계
- 코드 변경의 검증을 같은/더 약한 모델이 하면 자기 실수를 놓칠 가능성
- 최악의 경우 보안 설정(가드레일)까지 "개선"이라며 스스로 건드릴 위험
- 사람 개입 없는 반복 수정은 점진적 품질 저하로 이어질 수 있음

### 수동 트리거 워크플로 (야간 배치 대신)
```
평소: Qwen은 실제 서비스 트래픽만 처리, 자기수정 작업 없음
        ↓
당신이 "점검해줘" 라고 명시적으로 실행할 때만 (예: python maintain.py)
        ↓
Qwen이 변경 제안 → 격리된 git 브랜치에서 로컬 테스트
        ↓
필요하다고 판단되면 → 그때만 Claude Code/API 호출 (비용 발생 지점 명확화)
        ↓
통과 시 PR 생성 → 사람 리뷰·승인 → merge
```

- Claude API/Code 호출 이력을 간단한 로그로 남겨서 월별 사용량 대략 추적 (비용 통제용)

### 작업 유형별 담당 (Claude 역할 포함, 12장과 연결)
| 작업 유형 | 담당 |
|---|---|
| 의존성 업데이트, 로그 정리, 간단 버그 수정 | Qwen 자율 처리 (테스트 통과 조건) |
| 새 스킬 추가, 프롬프트 튜닝 | Qwen 제안 → 사람 승인 |
| 복잡한 리팩토링, 신규 기능 설계 | Claude Code로 직접 구현/리뷰 |
| 아키텍처 변경, 보안 설정 수정 | Claude Code + 사람 함께 검토 |
| 하네스(검증/가드레일) 로직 수정 | Claude Code + 사람, **로컬 모델 단독 절대 금지** |
| SNS 포스팅 등 리스크 큰 콘텐츠 최종 검수 | 선택적으로 Claude API 2차 검증 |

- 자율 수정 범위는 `skills/self-maintenance/SKILL.md`로 명시적으로 제한
- 모든 자율 수정은 별도 브랜치+PR로만, main 직접 push 금지
- 테스트 커버리지 있는 부분만 자율 수정 허용
- 자기수정 작업은 서비스 트래픽 적은 시간대에 배치로 실행 (메모리 경쟁 방지)

---

## 12. Claude의 역할

| 역할 | 설명 |
|---|---|
| 설계/자문 | 아키텍처 결정, 기술 선택, 트레이드오프 검토 (이 문서 작성 과정 자체) |
| 코드 작성 | Claude.ai 대화에서 스킬 파일(SKILL.md), 스크립트, 라우터 코드 직접 생성 |
| **Claude Code** | 실제 프로젝트 폴더에서 파일 읽기/수정/테스트 실행, Qwen이 만든 PR 리뷰, 복잡한 리팩토링·아키텍처 변경 구현 |
| **Claude API (선택적 임베드)** | 플랫폼 검증 단계(9장)에 상위 검증자로 연결 — 리스크 높은 작업만 선별적으로 2차 검증, 평상시 트래픽은 로컬 Qwen이 전담해 비용·리소스 절감 |
| MCP 연결 | GitHub MCP 등으로 Claude가 이슈 확인, PR 생성 등 로컬 시스템 작업 자동화 |
| **음성 피드백 (Voicebox MCP)** | Claude Code 유지보수 세션에서 Voicebox MCP 도구로 작업 결과를 클로닝된 음성으로 보고받기 가능 (20장 참고) |

### 현실적 워크플로
1. 평소 개발: Claude(대화 또는 Claude Code)와 함께 설계·초기 구현
2. 일상 운영: 로컬 Qwen이 실제 서비스 트래픽 처리 (비용 없음, 프라이버시 유지)
3. 유지보수/개선: 주기적으로 Claude Code 호출해 코드 품질 점검, 리팩토링, 스킬 작성
4. 위험도 높은 결정: Claude API를 선택적 검증 레이어로 사용

**정리**: Claude는 "매 순간 관여하는 운영자"가 아니라 "설계자·상위 검토자·조력자" 역할. 일상 처리는 로컬 Qwen, 복잡·고위험 결정에서만 Claude 개입하는 하이브리드 구조.

---

## 13. Docker 사용 여부

### 도입 이유
- 보안 샌드박싱(6장): 에이전트 실행 코드는 격리 환경 필수 (네트워크 차단, 파일시스템 제한)
- 오픈소스 도구(Open Notebook, SurfSense 등) 대부분 `docker-compose` 배포 표준
- 서비스 프로세스 격리 (FastAPI, 벡터DB, Redis, TTS 워커 등 하나가 죽어도 전체 영향 최소화)
- 환경 재현성 (다른 맥북 이전, 향후 배포 시 유리)

### 32GB 환경 주의점
- **Docker Desktop은 리눅스 VM 오버헤드**가 커서 메모리 부담 → **OrbStack으로 대체 권장** (Apple Silicon 네이티브 가상화, Docker 100% 호환, 훨씬 가벼움)

### 컴포넌트별 적용 여부
| 컴포넌트 | Docker 사용 | 이유 |
|---|---|---|
| 코드 실행 샌드박스 | 필수 | 보안 격리가 핵심 목적 |
| 벡터DB(LanceDB/Chroma), Redis | 권장 | 설치/버전관리 간편 |
| FastAPI 백엔드 | 선택 | 개발 중엔 로컬 실행이 디버깅 편함 |
| **Ollama(LLM 서빙)** | **비권장 (네이티브 실행)** | macOS 컨테이너의 GPU/Metal 패스스루 제한 → 속도 저하 |
| OpenClaw | 권장 | 필요시만 기동하는 서브모듈 관리에 유리 |

**핵심 원칙**: LLM 추론(Ollama)만 네이티브로 돌리고, 주변 인프라(큐/DB/샌드박스)만 컨테이너화하는 하이브리드 구조. 각 컨테이너에 `mem_limit` 명시로 Ollama가 쓸 메모리 여유를 항상 확보.

```yaml
# docker-compose.yml 예시 (Ollama 제외, 호스트 네이티브 실행)
services:
  redis:
    image: redis:alpine
    mem_limit: 512m
  vectordb:
    image: chromadb/chroma
    mem_limit: 1g
  code-sandbox:
    build: ./sandbox
    network_mode: none
    mem_limit: 2g
    read_only: true
  worker:
    build: ./worker
    depends_on: [redis]
    mem_limit: 1g
```

---

## 14. 전체 아키텍처 요약도

```
[외부 데이터] → 전처리 워커(OCR/Whisper, 컨테이너) → 임베딩 → LanceDB/SQLite(컨테이너)
                                                    ↓
[사용자 요청] → FastAPI → 작업 큐(Redis+RQ, 컨테이너) → 경량 라우터(9B, 네이티브 Ollama)
                                                    ↓
                                        스킬 판단(skills/ 로컬 폴더)
                                                    ↓
                            필요시 전문 모델 로드(30B, 네이티브 Ollama, 온디맨드)
                                                    ↓
                    [보안 게이트: 샌드박스 실행(컨테이너, network_mode:none) + 승인 단계]
                                                    ↓
                          [검증 단계: 출처/근거 일치 확인, 필요시 Claude API 2차 검증]
                                                    ↓
                            결과 반환 / SNS 송출(OpenClaw 서브모듈, 컨테이너)

[플랫폼 자체 유지보수]
  Qwen: 소규모 변경 자율 제안 → 브랜치/PR → 테스트
  Claude Code: 복잡한 변경 구현/리뷰, 하네스·보안 로직은 사람과 함께만
```

---

## 15. 데이터 저장 및 백업 (현재: 내장 2TB / 추후: 외장 SSD)

### 저장 위치 원칙 (현재 결정)
- **외장 SSD 구매 계획이 없어, 당분간 내장 2TB SSD에 데이터(벡터DB, 원본 파일, 스킬 실행 결과) 저장**
- 내장 SSD도 랜덤 I/O 성능은 우수(NVMe)해서 벡터 검색 성능상 문제 없음 — 애초에 외장 SSD 권장 이유는 "HDD 대비"였고, 내장이면 오히려 더 빠름
- 경로는 코드에 하드코딩하지 않고 `.env`로 분리 (예: `DATA_ROOT=/Users/username/ai-platform-data`) — 나중에 외장 SSD로 옮길 때 이 값만 바꾸면 되도록
- 볼륨 마운트 체크 로직은 현재 불필요 (내장 디스크는 항상 존재) — 외장 SSD 도입 시 재추가

### 저장 용량 관리 (내장 사용 시 특히 중요)
- 2TB 전체를 OS·앱·데이터가 공유하므로, 데이터 디렉터리 용량을 주기적으로 모니터링하는 스크립트 권장 (임베딩·원본파일·모델 캐시가 예상보다 빨리 찰 수 있음)
- Ollama 모델 파일 자체도 내장 디스크를 차지 (30B급 모델 하나당 수십GB) — 안 쓰는 모델은 정기적으로 정리
- 용량이 부족해지기 시작하면 그 시점이 외장 SSD 도입을 재검토할 신호

### 백업 원칙
- 내장 SSD 하나에만 데이터가 있으면 디스크 장애 시 전부 잃음 — 외장 SSD보다 오히려 **백업의 중요성이 더 큼**
- macOS Time Machine으로 내장 디스크 전체 정기 백업 설정 권장 (별도 백업용 외장 드라이브가 있다면 활용, 없다면 최소한 iCloud로 핵심 데이터만이라도)
- 코드/스킬은 Git으로 버전관리(비공개 GitHub 백업), **실제 수집 데이터(임베딩·원본파일)는 별도 백업 스크립트 필요** — 특히 내장 전용 환경에서는 이 부분을 가볍게 여기지 않는 게 중요

### 추후 외장 SSD 도입 시 전환 경로
- `.env`의 `DATA_ROOT` 값을 외장 SSD 경로로 변경
- 16장의 "시작 시 볼륨 마운트 확인" 로직 재추가
- 3-2-1 백업 원칙(원본+로컬백업+원격백업 중 최소 2곳) 적용 고려

---

## 16. 온디맨드 운영 원칙 ("상시 서버"가 아닌 "필요할 때 켜는 워크스테이션")

### 핵심 재정의
맥북을 상시 켜두지 않으므로, 지금까지의 "24/7 상시 대기 에이전트" 전제를 버리고 다음 원칙으로 설계:

1. **맥북을 켤 때** → 밀린 작업 재개 → 정상 서비스 (외장드라이브 도입 시 마운트 확인 단계 추가 예정)
2. **자동화는 시간 기반(cron)이 아니라 "맥북이 켜졌을 때" 기반(트리거)**으로 설계
3. **Claude 관련 작업은 전부 수동 호출**로 비용을 완전히 예측 가능하게 (11장과 연결)

### 구조 변경 필요 항목
- **작업 큐 영속성 필수**: Redis는 메모리 기반이라 맥북을 끄면 대기 작업이 유실될 수 있음 → Redis AOF/RDB 영속화 설정 또는 SQLite 기반 디스크 큐로 대체
- **"시작 시 복구" 패턴 도입**: 맥북을 켤 때마다 밀린 작업을 확인하고 이어서 처리

```python
# 앱 시작 시 (현재: 내장 저장소 기준, 외장 SSD 도입 시 check_external_drive_mounted() 재추가)
def on_startup():
    resume_pending_queue_jobs()  # 꺼져있던 동안 밀린 작업 확인
    log_downtime_gap()  # 참고용 다운타임 기록
```

- **외부정보 자동 수집도 "켜져 있을 때만"**: 예약 크롤링이 아니라 맥북을 켤 때 놓친 것 중 우선순위 높은 것만 골라 수집
- OpenClaw의 heartbeat처럼 상시 대기가 전제인 기능은 4장에서 다룬 대로 제약을 인지하고 사용

---

## 17. 실험 카탈로그 — 만들어볼 수 있는 서비스들

목표는 상용화가 아니라 **최신 AI 서비스를 로컬 환경에서 폭넓게 실험**하는 것. 모든 항목은 `skills/` 단위로 독립 구현하며, NotebookLM용으로 구축한 RAG/전처리 인프라를 재사용.

### A. 지식/문서 처리
- NotebookLM 스타일 소스 기반 QA + 팟캐스트 생성 (8장, 우선 구현)
- **개인 세컨드 브레인 (옵시디언 볼트 연동) — 22장 참고, 착수 난이도 가장 낮은 우선 후보**
- 웹 검색 증강 QA (Perplexity 스타일, SurfSense 참고)
- 회의/강의 녹음 요약 (Whisper 전사 → 화자분리 → 액션아이템 추출)

### B. 코딩 에이전트
- 자체 코딩 어시스턴트 (Qwen3-Coder 기반 미니 하네스 직접 구축)
- 코드베이스와 채팅 (프로젝트 전체 RAG 색인)
- PR 자동 리뷰 봇

### C. 음성/오디오
- **로컬 음성 비서 — Voicebox 도입으로 대부분 해결** (전역 받아쓰기 + 음성클로닝, 20장 참고)
- AI 팟캐스트 생성기 (Voicebox의 TTS 엔진 활용)
- 음악 분석/추천

### D. 이미지/영상 (멀티모달)
- 비전 기반 문서/영수증 처리 (가계부 자동화)
- 개인 사진 아카이브 자연어 검색
- 로컬 이미지 생성 실험 (Stable Diffusion 계열)
- YouTube/영상 요약기

### E. 언어 학습
- 회화 연습 파트너 (STT+TTS 실시간 발음/문법 교정)
- 원서/기사 기반 학습 (NotebookLM 인프라 재사용, 단어장 자동 생성)
- 번역+뉘앙스 설명 도구

### F. 개인 자동화/비서 (OpenClaw 계열, 16장 온디맨드 제약 적용)
- SNS 콘텐츠 파이프라인 (수집→요약→초안→승인 후 게시)
- 이메일 트리아지
- 개인 일정/할일 비서
- 일기/저널 도우미 (패턴·감정 트렌드 시각화, 진단은 하지 않음)

### G. 멀티에이전트/오케스트레이션 실험
- 역할 분리 협업 파이프라인 직접 구현 (리서치→작성→검토)
- 모델 라우팅 벤치마크 (9B/14B/30B 품질·속도 비교, 10장 평가체계와 연결)
- 컴퓨터 사용 에이전트 실험 (난이도 높음, 후순위)

### H. 데이터/생산성 분석
- 개인 재무 분석기 (카드 내역 CSV → 지출 패턴 리포트)
- 건강/운동 로그 비전 분석 (참고용, 의학적 조언 아님)

### 설계 원칙
1. 전부 스킬 단위 독립 구현 — 서로 간섭 없음
2. RAG/전처리 인프라 재사용 — NotebookLM 인프라가 B~F 대부분에서 공용
3. 성능보다 다양성 우선 — 일단 돌아가게 만들고 흥미로운 것만 다듬기
4. 클라우드 이식 대비 — 모든 스킬이 Ollama API(OpenAI 호환)로 모델 호출 → 나중에 엔드포인트만 교체하면 클라우드/랩탑 이전 가능

### 시작 순서 제안
1순위 NotebookLM(A, 인프라 검증) → 2순위 코딩 에이전트(B) 또는 음성 비서(C) → 3순위 언어학습(E, A 인프라 재사용) → 나머지(D/F/G/H)는 흥미에 따라 자유 선택

---

## 19. 2단계(과도기) 옵션 — 미니서버/상시운영

> **로드맵상 위치**: 상단 "전체 전략 로드맵" 참고. 이 장은 완전히 배제된 옵션이 아니라, **1단계(현재 맥북 온디맨드)에서 검증이 끝나고 "상시 자동화할 가치가 있다"는 판단이 설 때 착수하는 2단계 과도기 옵션**입니다. 수익성이 불확실한 상태에서 클라우드 비용을 미리 쓰는 대신, 하드웨어 투자로 저비용 실증을 하려는 목적. 지금(1단계) 아키텍처 결정과는 분리해서 다루며, 아직 착수 시점은 아닙니다.

### 상시 서비스 전환 시 고려해야 할 한계

하드웨어 스펙을 올려도 자동으로 해결되지 않는 구조적 한계들 — 1단계에서는 이 부담을 피하려 온디맨드로 운영하며, 2단계 착수 시에는 아래 항목을 하나씩 대응해야 함:

1. **모델 판단력 한계** — 메모리를 늘려도 로컬 30B~70B급 모델은 최상위 클라우드 모델 대비 엣지케이스 처리력이 낮음. 상시 운영은 예측 못한 입력이 계속 들어오는데, 사람이 매번 확인하기 어려움
2. **물리적 한계** — 동시성 제약(무거운 작업이 몰리면 결국 대기), 네트워크/전원 단일 장애점, 24/7 가동에 따른 하드웨어 마모(소비자용 부품이라 서버급 내구성 아님)
3. **보안 노출 확대** — 상시 인터넷 노출은 공격 표면을 질적으로 키움. 특히 SNS/메시지 연동을 상시로 돌리면 프롬프트 인젝션 등에 노출되는 시간이 24시간으로 늘어남. 방화벽·VPN·정기 패치 등 표준 서버 보안 관리 부담 추가
4. **신뢰성/자동복구 한계** — 상용 클라우드처럼 헬스체크·자동재시작·장애 알림 체계가 없으면, 장애를 본인이 우연히 발견해야 하는 구조
5. **1인 운영의 현실적 한계** — 자동 업데이트를 배제한 상태(11장)에서 상시 운영 시 로그/이상징후를 지속적으로 검토할 시간 확보가 어려움
6. **법적/윤리적 책임 확대** — SNS 자동 포스팅 등을 사람 확인 없이 상시로 돌리면 실수 콘텐츠 발송, 저작물 오용 리스크가 커짐
7. **숨은 비용** — Claude API 호출이 자동화 트리거로 인해 의도치 않게 늘어날 위험, 전기요금보다는 "계속 신경 써야 하는 대상"이라는 관리 비용이 더 큼

**2단계 착수 시 현실적 운영 목표**: 완전 무인 자동화가 아니라 "항상 켜져 있되 리스크 있는 액션은 사람 승인을 거치는 반자동 상시 서비스"를 상한선으로 설계. 이게 2→3단계(외부 클라우드 인프라) 전환 필요성을 판단하는 기준이 됨 — 위 한계가 실제 발목을 잡기 시작하면 3단계로 넘어갈 시점.

### 참고용 하드웨어 옵션 (2단계 착수 시 검토)

메모리 용량이 상시 서비스 전환 시 가장 중요한 스펙 — 32GB의 "모델 하나씩 교체" 구조에서 벗어나려면 128GB급 통합메모리가 근본적 해법.

| 등급 | 제품 | 스펙 | 특징 |
|---|---|---|---|
| 실속형 | Mac mini M4 Pro | 48GB 통합메모리, 512GB~8TB SSD(내장 비업그레이드, 외장 권장) | 조용함, 전력효율 최고, macOS라 개발환경 100% 호환. 27B급 모델 원활, 8B 모델 ~75tok/s |
| 균형형 | AMD Ryzen AI Max+ 395 미니PC (GMKtec EVO-X2, Framework Desktop 등) | 128GB LPDDR5X, 2TB NVMe, ~140W | 128GB 도달 가장 저렴한 경로($25.77/GB), 70B급 모델 원활, Linux 기반이라 Docker/서버 운영에 유리 |
| 성능 최우선 | Mac Studio M4 Max | 128GB 통합메모리, 546GB/s 대역폭 | EVO-X2 대비 2배 이상 빠른 대역폭, 현재 M1 Max와 동일 생태계라 이식성 100% |
| 최상위 | NVIDIA DGX Spark | 128GB 통합메모리, GB10 Grace Blackwell, CUDA 풀지원 | 최대 ~200B 모델까지, 파인튜닝 등 진지한 개발에 최적화, 프리미엄 가격 |

**전환 시 우선 추천**: 균형형(Ryzen AI Max+ 395, 128GB) — 메모리 확보 대비 비용 효율 최고, 낮은 전력 소비로 24/7 부담 적음. 개발 이식성을 최우선한다면 Mac Studio M4 Max.

### 전환 시 재검토 필요 항목
- 16장의 "시작 시 복구" 로직 → 헬스체크·자동재시작(watchdog, systemd/launchd 서비스 등록)으로 교체
- 6장 보안 체계 → 상시 노출 기준으로 강화 (방화벽, VPN, 정기 패치)
- 11장 Claude 수동 호출 → 자동 트리거 시 호출량 상한선/알림 장치 추가
- SNS 자동 포스팅 등 리스크 액션 → 상시 운영 시에도 최소한의 사람 승인 게이트는 유지 권장

---

## 20. TTS(음성 합성) 활용 — Voicebox 채택

오디오 오버뷰(8장 NotebookLM 기능), 음성 비서(17장 실험 카탈로그 C) 등에 쓰이는 핵심 요소.

### 채택 결정: jamiepine/voicebox
개별 TTS 엔진(Kokoro/Piper 등)을 직접 연동하는 대신, **오픈소스 로컬 AI 음성 스튜디오 Voicebox를 도입**하기로 결정. MIT 라이선스, TypeScript/Rust/Python/Tauri 스택, 활발한 릴리스 이력과 PROJECT_STATUS.md 기반의 투명한 로드맵 관리로 성숙도가 검증됨. (OpenClaw/OpenWorker와 달리 오케스트레이션 주도권을 요구하지 않는 스코프 좁은 유틸리티라 도입 리스크가 낮음 — 21장 판단 기준과 비교)

### Voicebox 핵심 기능
- 몇 초 샘플만으로 음성 클로닝, **7개 TTS 엔진 · 23개 언어** 내장 지원 (엔진 선택 로직이 이미 구현되어 있음)
- 전역 단축키로 어떤 앱에서든 받아쓰기(dictation) — 마이크 없이 텍스트→음성 변환도 가능
- **MCP 지원**: Claude Code, Cursor 등 MCP 인식 에이전트가 클로닝된 목소리로 답변을 말로 반환 가능 — 12장 Claude Code 활용과 직결
- 모든 처리가 로컬에서 완결 (음성 데이터 외부 유출 없음)
- 내부적으로 로컬 LLM을 두어 전사 결과 정리·음성 프로필 페르소나 처리

### 플랫폼 적용
- **20장 자체 구현 계획 대체**: `skills/audio-overview/tts_synthesis.py`를 직접 짜는 대신 Voicebox의 엔진 선택 기능을 그대로 활용
- **17장 C(음성 비서 실험) 대부분 해결**: 전역 받아쓰기 + 클로닝이 이미 완성된 형태로 제공되어 별도 구현 불필요
- **12장(Claude 역할)과 연결**: Claude Code로 유지보수 작업 시 MCP를 통해 결과를 음성 피드백으로 받는 활용 가능
- **22장(옵시디언)과 조합**: 받아쓰기로 노트를 빠르게 작성해 볼트에 쌓는 입력 경로로 활용 가능

### 도입 시 확인 사항
- Tauri 기반 데스크톱 앱이라 GUI 필요 — 헤드리스 백엔드가 아니라 별도 앱으로 띄워 MCP/API로 연동하는 구조
- 내부 로컬 LLM이 Qwen과 별개로 메모리를 쓰는지 실제 설치 후 확인 필요 (32GB 예산 영향 체크)
- 16장 온디맨드 운영 원칙과 자연스럽게 부합 — 맥북 켤 때 함께 실행

### 참고 — 개별 TTS 엔진 특성 (Voicebox 내부에 포함되거나 필요시 개별 검토용)
| 모델 | 특징 | 라이선스 |
|---|---|---|
| Kokoro-82M | 초경량(~350MB), CPU로 실시간보다 빠름, 54개 음성/8개 언어 | Apache 2.0 |
| Piper | 최경량, 초저지연(~40ms) | MIT |
| Qwen3-TTS | 다국어 10개 언어, 3초 음성 클로닝 | Apache 2.0 |
| Coqui XTTS v2 | 6초 샘플로 가장 자연스러운 클로닝 | CPML(비상업용) |
| Chatterbox | 감정 표현(웃음/울음 등) | MIT |
| **Supertonic** (Supertone) | 99M 초경량, 한국어 포함 31개 언어, M4 Pro 기준 실시간 대비 최대 167배 속도, GPU 불필요, 44.1kHz 고음질, 숫자/통화/전화번호 등 까다로운 텍스트 정확 처리 | 코드 MIT / 모델 가중치 OpenRAIL-M(개인 사용 무료, 상업적 사용시 유해·사칭 금지 및 출처표기 제약) |

### Supertonic 개별 채택 — 한국어·초고속 응답용
Voicebox(클로닝 중심 종합 스튜디오)와 경쟁이 아닌 **보완 관계**로 추가 채택. 이 저장소는 고정 음성(fixed-voice) 로컬 TTS에 특화되어 있고 공식 클로닝 파이프라인은 없음(클로닝 필요시 별도 Voice Builder 도구 이용).

**적용 위치**
- **외국어 학습 에이전트의 한국어 콘텐츠** — 한국 기업(Supertone) 제품이라 한국어 발음/억양 품질 신뢰도가 높음, 원어민 발음 예시 등에 우선 고려
- **빠른 UI 피드백음/알림** — 초경량·초고속 특성상 팟캐스트 같은 긴 콘텐츠보다 "작업 완료 알림", "짧은 확인 메시지" 같은 즉각적 음성 응답에 최적
- **숫자·통화 등 정확도가 중요한 음성 출력** — 재무 분석기(17장 H) 결과를 음성으로 요약하는 등, 정확한 수치 낭독이 필요한 스킬에 강점

```
pip install supertonic
```
`pip install`만으로 바로 사용 가능해, Voicebox 앱을 띄우지 않고 특정 스킬 스크립트 안에서 가볍게 직접 호출하는 용도로도 적합.

### 적용 원칙
- LLM(9B~30B) 대비 TTS 엔진 자체의 메모리 부담은 미미 → "모델 상주 최소화" 전략(2장)에서 예외로 두고 상시 로딩 가능
- 별도 프로세스(Voicebox 앱)로 분리되어 LLM 추론 서버와 자연스럽게 격리됨 (7장 전처리 원칙과 동일한 방향)
- Supertonic은 `pip install`로 가벼워 특정 스킬 내부에 직접 호출로 내장 가능 (별도 앱 프로세스 불필요)

---

## 21. OpenWorker 평가 — 승인 체계 패턴만 차용

### 개요
Andrew Ng이 2026년 7월 공개한 오픈소스 로컬-퍼스트 데스크톱 AI 에이전트. FastAPI+React 기반, Ollama 완전 로컬 지원, MIT 라이선스. "채팅"이 아니라 문서/스프레드시트/캘린더 등 완성된 결과물을 반환하는 방식으로 설계됨.

### 핵심 요소 — 승인/권한 체계
- 모든 도구 호출을 `read`(부작용 없음) / `write_local`(작업공간 수정) / `exec`(명령 실행) / `external`(외부 부작용) 4단계 위험 등급으로 분류
- `discuss`/`plan`(읽기전용) / `interactive`(기본, 위험 작업 전 승인 요청) / `auto`(경로 범위 내 자동 허용) / `custom`(지정 도구만 자동승인) 5가지 권한 모드

### 채택 여부 판단
**전체 프레임워크 도입은 보류, 승인/권한 분류 패턴만 차용하기로 결정.**

이유:
- 출시 1개월 남짓의 신생 프로젝트 — 보안·안정성 검증 기간 필요 (OpenClaw 초기 사고 사례 참고)
- 3장 원칙("인프라는 빌리고, 로직은 짓는다")상 오케스트레이션 주도권까지 넘기는 건 과함
- 2장에서 설계한 "경량 라우터+온디맨드 전문모델 교체"라는 32GB 특화 메모리 최적화 구조와 OpenWorker 자체 에이전트 루프가 충돌할 가능성
- 자체 모델 라우팅 로직과 중복되어 의사결정 주체가 애매해질 위험

### 실제 적용
- 승인/권한 분류 체계(4등급 위험 태깅 + 승인 모드)를 6장 보안 체계 항목 3번으로 이식 완료
- 프레임워크 자체는 3~6개월 안정성 관찰 후 재평가 (필요시 19장 미니서버 전환 시점에 재검토)

---

## 22. Obsidian(옵시디언) 연동

### 볼트(Vault)란
옵시디언에서 노트 전체를 담는 **하나의 일반 폴더**를 가리키는 용어. 특별한 저장 형식이 아니라, 옵시디언이 그 폴더를 열면 안의 `.md` 파일들을 서로 연결된 노트 묶음으로 인식해서 관리해주는 것뿐임.

```
MyVault/                    ← 이 폴더 자체가 "볼트"
  .obsidian/                ← 옵시디언 설정(테마, 플러그인 등) 저장되는 숨김 폴더
  일상노트/2026-08-05.md
  프로젝트/AI플랫폼구축.md
  참고자료/논문요약.md
```

- 폴더 하나 = 볼트 하나. 여러 볼트를 만들면 서로 완전히 독립적으로 취급됨
- 이 구조 덕분에 플랫폼 연동 시 **별도 API 없이 파일시스템 경로 스캔만으로 접근 가능** (아래 구현 구조 참고)

### 개요
로컬 마크다운 파일 기반 개인 지식관리(PKM) 앱. 모든 노트가 순수 텍스트 `.md` 파일로 기기에 저장되며, 위키링크(`[[노트명]]`)로 노트 간 양방향 연결·그래프 뷰 제공. **유료 기능(Obsidian Sync, Publish) 사용 계획 없음 — 볼트는 로컬(현재는 내장 2TB SSD)에만 존재.**

### 플랫폼 적용 우선순위 — 높음 (8장 NotebookLM 기능의 첫 실 데이터로 최적)

**도입 확정 — 초기 단계 판단 근거**
현재 플랫폼 구축 아주 초기 단계라는 점이 도입 우선순위를 더 높이는 이유:
- 스킬 구조·RAG 파이프라인·벡터DB 연동을 처음 만드는 시점에는, 전처리가 복잡한 데이터(영상 등)로 시작하면 파이프라인 버그인지 데이터 문제인지 구분이 어려움 — 전처리가 거의 없는 옵시디언 노트로 "끝까지 도는 흐름"부터 검증하는 게 효율적
- 초기 아키텍처를 "가장 단순한 케이스" 기준으로 잡아두면, 이후 이미지/영상 같은 복잡한 타입 추가 시 구조를 갈아엎지 않고 확장만 하면 됨
- 지금부터 플랫폼 구축 과정(회의록, 설계 결정, 학습 내용)을 옵시디언에 기록하기 시작하면, 몇 달 뒤 검색할 가치가 있는 볼트가 자연스럽게 만들어짐

**시딩(seeding) 제안**: 노트가 거의 없는 상태에서 바로 통합하면 검색 결과가 빈약해 파이프라인 정상 동작 여부 판단이 어려움 → 테스트용으로 노트 10~20개를 먼저 만들어두거나(플랫폼 설계 회의록, 이 대화 정리 등도 좋은 시작 자료), 2~3주 실사용 후 볼트를 채운 뒤 통합 작업 착수 권장

**다른 데이터 타입 대비 유리한 이유**
- 이미 순수 텍스트라 7장의 문서 전처리(OCR, 파싱, 노이즈 제거) 단계가 거의 불필요 — 파일을 읽는 즉시 청킹 단계로 진입 가능
- 볼트가 그냥 로컬 폴더이므로 별도 API 연동 없이 경로 스캔만으로 전체 노트 접근 가능
- 이미 쌓아둔 노트가 있다면 NotebookLM 소스 기반 QA(8장)를 가장 빠르게 실제 데이터로 검증 가능

### 구현 구조
```
skills/obsidian-sync/
  SKILL.md
  scan_vault.py     # 볼트 폴더(.env에 경로 설정, 현재 내장 SSD 내) 스캔, .md 파일 목록화
  parse_links.py     # [[위키링크]] 파싱 → 노트 간 관계를 메타데이터로 저장
```

- 청킹 → 임베딩 → LanceDB 저장 시 위키링크 관계를 메타데이터로 함께 저장하면 "연결된 다른 노트도 같이 찾기" 질의 가능
- **증분 동기화 필요**: 노트 수정시간(mtime) 체크해서 변경된 파일만 재임베딩 (매번 전체 재처리 방지)
- 유료 Sync 없이 로컬 볼트만 사용하므로 16장 온디맨드 운영 원칙과 자연스럽게 부합 (맥북 켤 때 볼트도 함께 로컬에 있음)

### 참고 (직접 구현 시 아이디어 소스)
커뮤니티 플러그인 Smart Connections(로컬 임베딩 시맨틱 검색) + Copilot for Obsidian(볼트 컨텍스트 채팅)을 Ollama 백엔드에 연결하는 방식이 이미 널리 쓰이고 있어, 자체 파이프라인 설계 시 참고할 만함 (단, 21장 원칙대로 프레임워크 전체 도입이 아니라 아이디어만 참고).

---

## 23. OfficeCLI 채택 / AionUi 평가

같은 조직(iOfficeAI)의 두 프로젝트지만 스코프가 달라 판단이 갈림.

### OfficeCLI — 적극 채택 (Supertonic과 같은 성격)

AI 에이전트를 위해 설계된 Word/Excel/PowerPoint 조작 CLI. Office 설치 없이 단일 바이너리로 문서 읽기/쓰기/수정.

- 내장 HTML 렌더링 엔진으로 문서를 고충실도로 재현해 "렌더링→보기→수정" 루프 완성 (문서를 이미지로 렌더링해 AI가 결과를 시각적으로 검증 가능)
- Word(문단·표·차트·수식·워터마크), Excel(셀·수식 150+·피벗테이블·조건부서식), PowerPoint(슬라이드·애니메이션·3D모델)까지 DOM 방식 경로로 조작
- **SKILL.md 자체 내장** (약 239줄, 8천 토큰) — 당신의 스킬 구조에 그대로 배치 가능
- 보안 스캔(의존성 취약점 + 프롬프트 인젝션 휴리스틱) 통과, 고위험 이슈 없음
- 완전 무료, 상업적 사용 제약 없는 permissive 라이선스

**채택 판단 근거**: 오케스트레이션이 아닌 "문서 조작"이라는 스코프 좁은 유틸리티 — 3장 원칙(인프라는 빌리고)에 부합, 도입 리스크 낮음. 이미 SKILL.md 형식이라 `skills/office-docs/`에 그대로 배치하면 즉시 작동.

**적용처**
- 17장 H(재무 분석기) — CSV 분석 결과를 Excel 피벗테이블/차트로 자동 생성
- 8장(NotebookLM 리포트) — 요약 결과를 Word/PowerPoint로 출력
- 17장 B(코딩 에이전트) — 기술 문서를 Word로 정리

```
skills/office-docs/
  SKILL.md   # OfficeCLI 자체 제공 SKILL.md 그대로 사용
```

### AionUi — 신중 접근 (OpenClaw/OpenWorker와 동일 원칙)

멀티 에이전트 데스크톱 "Cowork" 앱. Claude Code·Codex·Qwen Code·OpenClaw 등 20개 이상 CLI 에이전트를 하나의 인터페이스에서 통합 관리.

- Ollama 완전 로컬 지원, `skills/` 디렉터리 시스템(당신 구조와 개념적으로 유사), MCP 통합 관리(도구 1회 설정 → 여러 에이전트 자동 동기화)
- 24/7 자동화 + 메신저 연동(Telegram, Lark, DingTalk, WeChat) — OpenClaw와 역할 중복
- 모든 데이터 로컬 SQLite 저장, 완전 오프라인 가능

**채택 판단 근거**: AionUi 자체가 "여러 에이전트를 조율하는 상위 레이어" 역할을 하려 하는데, 이는 FastAPI로 직접 구축 중인 라우팅/스킬 판단 로직과 겹침 — 21장의 "오케스트레이션 주도권은 넘기지 않는다" 원칙 그대로 적용.

**결론**: 메인 플랫폼에 통합하지 않음. 필요시 **개인 작업용 보조 데스크톱 툴**로만 병행 사용 (Claude Code/Qwen Code를 나란히 띄워 비교 작업할 때 등) — 메인 FastAPI 백엔드·오케스트레이션과는 분리 유지.

---

## 24. GitHub 40,000+ 스타 오픈소스 평가

"스타 수 = 내 아키텍처 적합도"가 아니라는 전제 하에, 대형 인기 프로젝트를 21/23장과 동일한 원칙(오케스트레이션 주도권 유지, 좁은 스코프 유틸리티 우선 채택)으로 평가.

| 프로젝트 | 스타 수 | 성격 | 채택 판단 |
|---|---|---|---|
| ComfyUI | ~106,000 | 노드 기반 이미지/영상 생성 워크플로 | ✅ 채택 |
| vLLM | ~68,000 | 고성능 LLM 추론 서버(CUDA 중심) | ❌ 부적합 |
| Dify / Langflow | 136,000 / 146,000 | 시각적 에이전트 오케스트레이션 플랫폼 | ⚠️ 신중 접근 |
| LangChain | 다수 | RAG/체인 구성 라이브러리 | ⚠️ 부분 활용(라이브러리만) |
| nanochat (Karpathy) | ~55,000 | 토크나이징~채팅UI 전체 LLM 학습 파이프라인 미니멀 구현 | ✅ 학습용 채택 |

### ComfyUI — 채택
- 이미지 생성 파이프라인 사실상 업계 표준, 노드 기반으로 세밀한 제어 가능
- 17장 D(이미지/영상 실험)의 "로컬 이미지 생성 실험" 항목을 이 도구로 구현
- 오케스트레이션 겹침 없음(좁은 스코프) — 3장 원칙 부합, Apple Silicon MPS 백엔드 지원
- API 모드로 실행해 FastAPI 스킬에서 호출하는 방식으로 통합

### vLLM — 부적합
CUDA(NVIDIA GPU) 최적화 중심이라 Apple Silicon에서 이점 거의 없음. 이미 채택한 Ollama/MLX가 Metal 가속을 더 잘 활용 — 지금 스택에서 도입 이유 없음.

### Dify / Langflow — 신중 접근 (OpenClaw/AionUi와 동일 원칙)
- 시각적 워크플로 빌더형 에이전트 오케스트레이션 도구로, FastAPI로 직접 구축 중인 스킬 라우팅/하네스와 역할이 겹침
- UI 기반 워크플로 빌더는 2장의 세밀한 32GB 메모리 최적화를 직접 제어하기 어렵게 만들 가능성
- **활용 방향**: 전체 도입 대신 RAG/에이전트 파이프라인 설계 참고 자료로만 활용

### LangChain — 부분 활용
프레임워크 전체(체인/에이전트 실행 로직)를 오케스트레이션 엔진으로 들이지 않고, **텍스트 스플리터·리트리버 등 개별 컴포넌트만 라이브러리로 차용** — 7장 청킹 로직 구현 시 바퀴를 다시 만들지 않는 선택지로 활용 가능.

### nanochat — 학습용 채택
- 토크나이징부터 사전학습·파인튜닝·평가·추론·채팅UI까지 전체 파이프라인을 최소한이고 해킹하기 쉬운 형태로 담은 저장소(GPT-2급 학습 비용 약 $48 기준, 원래 클라우드 GPU 설계)
- 17장 G(멀티에이전트/오케스트레이션 실험)에 정확히 부합 — "모델이 어떻게 학습되는지" 내부 구조를 이해하는 학습 목적에 최적
- M1 Max에서 실제 학습 실행은 클라우드 GPU 대비 느릴 수 있어, **"실행"보다 "코드를 읽고 구조를 이해하는 학습 자료"**로 우선 활용 권장

### 종합
신규 채택은 **ComfyUI(이미지 생성)**, **nanochat(학습용)** 두 가지. vLLM·Dify·Langflow는 스타 수와 무관하게 각각 하드웨어 부적합, 오케스트레이션 주도권 문제로 신중 접근 유지.

---

## 25. 콘텐츠 생성 에이전트 — 저장 콘텐츠 활용 단계

지금까지는 "AI 기능 축적"(지식 처리·코딩·음성·문서 관리) 중심이었다면, 이제 **저장된 콘텐츠를 활용해 새 콘텐츠(번역·이미지·SNS·사진·영상·PPT/PDF)를 생성하는 에이전트** 단계로 전환. 새 오픈소스를 대거 도입하기보다 **기존 채택 도구의 조합으로 대부분 해결**되는 게 핵심 발견.

### 카테고리별 정리

| 카테고리 | 상태 | 신규 도입 |
|---|---|---|
| 번역 | Qwen3(다국어 100+ 언어 지원)로 충분 | 불필요 — `skills/content-generation/translation/` 신규 스킬만 작성 |
| 이미지 생성 | ComfyUI(24장 기채택) | rembg(배경제거, MIT), Real-ESRGAN(업스케일링, BSD) 보조 추가 |
| SNS 콘텐츠 | 기존 도구 조합(Qwen+ComfyUI+Voicebox/Supertonic+OpenClaw) | 불필요 |
| 사진 후처리 | — | rembg, Real-ESRGAN |
| **동영상 생성** | 확산 생성은 하드웨어 한계 / **숏폼 조립은 MoneyPrinterTurbo로 대안 확보** | 확산 모델 로컬 대안 없음, 스톡영상 조합 방식은 가능 (아래 상세) |
| PPT/PDF 생성 | OfficeCLI(23장 기채택) | 불필요 |

### 번역
Qwen3가 2026년 기준 로컬 다국어 번역의 사실상 기본 선택지로 자리잡음 — 별도 번역 전용 모델 불필요. 저자원 언어·대량 배치 번역이 필요할 때만 NLLB-200(Meta, 6억~546억 파라미터대 경량 모델)을 보조로 고려.

### 이미지 생성/편집
- ComfyUI(24장)가 텍스트→이미지 기본 파이프라인 담당
- **rembg**: 배경 제거 자동화, SNS용 상품/인물 사진에 활용
- **Real-ESRGAN**: 저해상도 이미지를 SNS 게시용으로 업스케일
- 둘 다 스코프 좁은 유틸리티 — `skills/content-generation/image-postprocess/`로 통합

### 동영상 생성 — ⚠️ 근본 한계는 있으나 부분 대안 확보 (MoneyPrinterTurbo)
2026년 주요 오픈소스 **영상 생성**(확산 모델) 도구(Wan 2.2, HunyuanVideo, LTX-2.3, Mochi 1 등)는 전부 NVIDIA GPU 전제(16~24GB VRAM)이며, Apple Silicon 지원은 ComfyUI에서조차 여전히 일관성이 떨어져 맥 사용자에게는 종종 막다른 길이 됨. 이 근본 한계는 여전히 유효.

**부분 대안: MoneyPrinterTurbo (MIT, 스톡영상 조합 방식)**
- 영상을 확산 모델로 "생성"하지 않고 **LLM(스크립트)+스톡영상(Pexels API)+TTS+자막+MoviePy/FFmpeg 합성**으로 조립 — GPU 불필요(있으면 전사·배치 속도만 향상), Ollama/Qwen 네이티브 지원, REST API로 FastAPI 스킬 통합 용이
- 창작 영상(확산 생성)은 아니지만 **숏폼 요약/정보성 콘텐츠 자동화**(유튜브 쇼츠·틱톡 등)에는 실용적 — 28장 수익화 콘텐츠 파이프라인의 실질적 해법
- 채택 시 28장 가이드라인 그대로 적용: 6장 3번 승인 게이트(`external` 등급, 자동 발행 금지), AI 생성 콘텐츠 표기 의무, 플랫폼 스팸/저품질 반복 콘텐츠 정책 위험 — 물량보다 품질 우선
- Pexels API 키 필요(외부 의존, 무료 등급 한도 확인), "이름의 과장"에 낚이지 않고 레퍼런스 파이프라인으로 냉정하게 평가할 것

```
skills/content-generation/video-shorts/
  SKILL.md
  moneyprinterturbo_client.py   # REST API 호출, Qwen 스크립트 생성 + Voicebox/Supertonic TTS 연동
```

**대응 방향 (본격 생성형 영상 필요 시)**
- 1단계(현재)에서 확산 기반 영상 생성은 낮은 우선순위 유지, 숏폼 조립은 MoneyPrinterTurbo로 충당
- 본격적인 확산 영상 생성이 필요해지면 **19장 2단계(미니서버) 전환 시 NVIDIA GPU 옵션을 재검토하는 근거**로 삼음 (기존 검토는 Ryzen AI Max 계열 중심이었으나, 영상 생성이 핵심이라면 NVIDIA 계열 재비교 필요)
- 또는 이 기능만 예외적으로 클라우드 API(Runway, Luma 등)를 필요시 호출하는 부분적 하이브리드도 고려 가능 (3단계를 전체적으로 당기지 않고 이 기능만 외부화)

### PPT/PDF 파일 생성
23장에서 채택한 OfficeCLI가 이미 해결 — PowerPoint(슬라이드·차트·애니메이션), Word, Excel, PDF(HTML/PNG 렌더링 및 exporter 플러그인) 전부 커버. `skills/content-generation/office-docs/`에서 그대로 호출.

---

## 26. 기능별 아키텍처 분리 원칙

### 핵심 원칙: 무거운 전문 도구만 프로세스 분리, 오케스트레이션은 통합 유지

**분리 대상 (이미 그렇게 되어 있음)**
- Ollama(LLM 추론), ComfyUI(이미지 생성), Voicebox(음성), OfficeCLI(문서) — 각각 독립 프로세스/바이너리, API·CLI로 호출

**분리하면 안 되는 것**
- 스킬 라우팅/의사결정 로직, 작업 큐, 메모리 예산 관리(2장), 승인/보안 게이트(6장)

### 완전 분리(기능별 마이크로서비스)를 피하는 이유
1. **2장 메모리 최적화 원칙 붕괴** — "경량 라우터 상시 + 전문 모델 온디맨드 교체"는 하나의 중앙 관리자가 전체 32GB 예산을 파악하고 있어야 작동. 서비스별로 독립 앱이 되면 각자 모델을 로드하려 들며 메모리 충돌
2. **큐/승인 체계 중복** — 서비스마다 따로 구현하면 일관성 붕괴, 유지보수 부담 배증
3. **1인 운영 한계 조기 도달** — 서비스 수만큼 배포·모니터링 지점이 늘어나 19장에서 짚은 1인 운영 부담을 스스로 앞당김

### 실제 적용: 완전 분리 대신 스킬 폴더 계층화
아키텍처 분리가 아니라 **네임스페이스(코드 조직) 분리**로 달성. 실행은 여전히 하나의 FastAPI + 하나의 큐 + 하나의 라우터가 담당.

```
skills/
  knowledge/               # 8장 NotebookLM, 22장 옵시디언
    notebook-qa/
    obsidian-sync/
  coding/                   # 17장 B
    code-review/
  content-generation/       # 25장 신규 영역
    translation/
    image-postprocess/       # rembg, Real-ESRGAN
    office-docs/              # OfficeCLI
    sns-publish/
  audio/                    # 20장
    audio-overview/
  video/                    # 25장, 보류 중
```

### 예외적 물리 분리를 고려할 시점
- **2단계(미니서버) 전환 시**: 128GB급으로 리소스가 늘어나면 일부 기능(예: 영상 생성)을 별도 GPU 박스로 실제 물리 분리하는 것이 합리적 — 1단계(32GB)에서는 시기상조
- **특정 도구가 잦은 크래시로 독립 재시작이 필요할 때**: 이미 ComfyUI/Voicebox처럼 별도 프로세스로 격리된 것들이 해당 사례

---

## 27. 수익화 모델 분석 — 소비자 AI 서비스 사례와 3단계 전환 요건

### 현재 수익화되는 AI 서비스 3원형
2026년 AI 앱 수익화는 세 패턴으로 군집됨: **구독 중심형**(Character.AI, Replika, Claude, Cursor), **광고 결합 구독형**(ChatGPT, M365 경유 Copilot), **마켓플레이스형**(Poe). 8개 상위 앱 매출 범위는 Character.AI의 3천만 달러부터 Anthropic 연환산 140억 달러, Cursor 연환산 20억 달러까지.

**핵심 인사이트**: 2025~2026년 가장 빠른 성장은 (a) 기업가격 생산성 앱(Cursor, Copilot, Claude Code)과 (b) 무료 등급 위에 광고를 결합한 소비자 앱(ChatGPT)에서 나옴 — 단일 레버 순수 소비자 구독은 빠르게 정체되는 반면, 마켓플레이스·광고결합형은 더 멀리 감.

### 실제 수익 모델 구조
| 모델 | 예시 | 특징 |
|---|---|---|
| 구독 | Calm, ChatGPT Plus, Claude | 미국 앱스토어 소비자 매출의 약 80%가 구독 |
| 광고 결합 구독 | ChatGPT(무료 등급+스폰서) | 무료층으로 규모 확대, 유료 전환 유도 |
| 사용량 기반 | API/토큰 과금 | B2B·기술 사용자층에 효과적, 순수 소비자용은 상한선/번들 필요 |
| 하이브리드 | 상위권 대부분 | 60% 이상이 여러 수익원 결합 |
| 엔터프라이즈 라이선스 | Copilot, Cursor Team | 판매주기 김, 계약단가 높음 |

### 플랫폼 스킬 ↔ 수익 원형 매칭
| 스킬 | 매칭 원형 | 잠재력 |
|---|---|---|
| 17장 B 코딩 에이전트 | Cursor/Claude Code형(엔터프라이즈 생산성) | **가장 유망** — 검증된 확장성 |
| 8장 NotebookLM 기능 | Perplexity/구독형 | 개인 지식관리 도구로 구독 전환 가능 |
| 17장 E 외국어학습 | Duolingo형(구독+마이크로결제) | 게이미피케이션 요소 추가 시 유효 |
| 25장 콘텐츠생성 | 사용량 기반(크레딧) | B2B 마케터·크리에이터 니치 |

**주의**: 절대 매출 숫자를 목표로 삼지 말고 구조적으로 유사한 원형에 매칭할 것 — 컴패니언 앱이 Cursor의 ARR을, 코드 어시스턴트가 Character.AI의 MAU를 만들지 못함.

### 3단계(수익화) 전환 시 필요한 것
**기술적 요건**
- 멀티테넌시 아키텍처 전환 (현재 "개인 전용" 전제를 완전히 뒤집어야 함)
- 실시간 사용량 측정/entitlement 시스템 — 모델이 아니라 이 강제 체계가 매출을 만듦
- 결제 연동 (Stripe 또는 앱스토어 인앱결제 15~30% 수수료, 국내는 PG사 연동)
- 외부 클라우드 인프라 (19장 — 미니서버로는 SLA·동시접속에 근본적 한계)

**비기술적 요건**
- 사업자 등록, 이용약관·개인정보처리방침 (통신판매업 신고 등)
- 고객 지원 채널
- 가격 전략: 2~3개 등급이 기본(월간/연간/평생), 등급이 많으면 선택 마비

### 로드맵과의 연결
1단계(현재, 스킬 축적) → 2단계(미니서버, 상시 자동화 실증) → **3단계(수익화)**에서 위 요건을 갖춰 외부 클라우드로 전면 이전. 코딩 에이전트 계열이 가장 검증된 구조와 맞닿아 있으나, 1단계 설계 자체를 지금부터 이를 염두에 두고 복잡하게 만들 필요는 없음.

---

## 28. 콘텐츠 기반 클릭/제휴 수익화 — 지금 단계에서도 착수 가능

27장의 SaaS형 수익화와 달리, **어필리에이트(제휴마케팅)·광고 클릭 수익은 "남에게 서비스를 파는 것"이 아니라 "내 콘텐츠 채널이 트래픽을 만드는 것"**이라 멀티테넌시·결제 연동이 불필요 — 4장(SNS 연동)·25장(콘텐츠 생성)·17장 F(개인 자동화)의 자연스러운 확장으로 **지금 단계에서도 착수 가능**. 해외구매대행·쇼핑몰(결제·재고·통관 직접 처리)과는 성격이 다름 — 그것들은 여전히 "설계 밖" 별도 프로젝트로 유지.

### 채널별 정리

**쿠팡파트너스 (제휴 어필리에이트)**
- API로 카테고리별 인기상품 조회 → 딥링크 생성 → 콘텐츠 자동 초안 작성 → 게시가 이미 실사용되는 패턴
- 클릭 시 쿠키가 남아 유효기간 내 다른 상품을 구매해도 커미션 지급되는 구조

**네이버 애드포스트 (블로그 광고 수익)**
- 네이버 블로그에 광고를 게재하고 노출·클릭 수에 따라 수익 배분받는 네이버 공식 프로그램
- 가입 조건: 블로그 개설 30일 이상, 게시글 20~30개 이상, 주제 일관성 유지, 정보성 글 비중 높음(일기형 지양)
- 미디어 등록 후 네이버 검수(영업일 5일) 통과해야 광고 게재 시작
- RPM(1000뷰당 수익)이 구글 애드센스의 1/5~1/10 수준으로 진입장벽은 낮지만 단가는 낮음 — 애드포스트로 시작해 트래픽이 쌓이면 워드프레스+애드센스(4장에서 이미 검토한 채널)로 확장하는 경로가 일반적
- 애드포스트 수익의 대부분은 트래픽에서 나오므로, 검색 유입을 늘리는 SEO 최적화가 병행되어야 함

### 법적 고지 의무 — 자동화에 반드시 포함
- 공정거래위원회 추천·보증 표시광고 심사지침에 따라 제휴 링크 포함 콘텐츠는 **"이 포스팅은 쿠팡 파트너스 활동의 일환으로 일정액의 수수료를 제공받습니다"** 같은 고지 문구가 법적 의무
- **2026년부터 AI 생성 콘텐츠 표기 의무가 강화**되어, "AI 생성 콘텐츠" 고지도 함께 필요
- 이 두 고지는 선택이 아니라 콘텐츠 발행 파이프라인의 **필수 하드코딩 단계**로 구현 — 6장 보안체계의 "법적/윤리적 책임" 항목과 직결

### 구현 구조
```
skills/monetization/affiliate-content/
  SKILL.md
  coupang_partners_api.py     # 카테고리별 인기상품 조회, 딥링크 생성
  naver_adpost_setup.py        # 애드포스트 미디어 등록/검수 상태 관리
  content_generator.py          # Qwen이 상품평/추천글/블로그 초안 작성
  disclosure_injector.py        # 제휴 고지 + AI 생성 고지 자동 삽입 (필수 단계)
```

### 콘텐츠 파이프라인 (기존 스킬 조합)
```
쿠팡파트너스 API(상품 조회) / 블로그 주제 기획
  → Qwen(리뷰·추천·정보성 콘텐츠 생성, 25장)
  → 법정 고지(제휴+AI생성) 자동 삽입 — 필수, 생략 불가
  → OfficeCLI/ComfyUI/rembg로 이미지·카드뉴스 생성
  → 승인 게이트(6장 3번, external 등급 — 반드시 사람 확인)
  → 네이버블로그/워드프레스 API 게시(4장)
```

### 성과 추적
클릭/전환/조회수 데이터를 SQLite에 기록해 어떤 콘텐츠 유형이 실제 수익을 내는지 분석하는 스킬 추가 고려 (17장 H 데이터 분석과 유사 패턴).

### 주의할 점
- **6장 3번(승인/권한 분류) 필수 적용**: 제휴 링크 포함 콘텐츠 발행은 `external` 등급 — 자동 발행 금지, 항상 사람 승인
- AI가 과장 광고·부정확한 정보를 생성해 발행하면 법적 책임으로 직결 — 검수 없는 발행은 19장 "법적/윤리적 책임 확대" 리스크의 실제 사례
- 저품질 대량 생성 콘텐츠는 검색엔진·플랫폼 알고리즘 페널티 위험 — 물량보다 품질·주제 일관성 우선 (네이버 애드포스트 승인 조건 자체가 이를 요구)

---

## 29. Agent Reach 평가 — 조건부 도입

AI 에이전트에게 소셜/웹 플랫폼(Twitter, Reddit, YouTube, GitHub, Bilibili, 샤오홍슈 등 12개+) 읽기·검색 접근을 부여하는 CLI 오케스트레이션 도구. MIT 라이선스, 로컬 우선.

### 개요
- 래퍼가 아니라 설치·헬스체크·설정 도구 — 설치 후 에이전트가 yt-dlp, Jina Reader, Exa 등 업스트림 오픈소스 도구를 직접 호출
- API 비용 없는 백엔드 구성, 서버 환경에서 Reddit/Bilibili 접근 시에만 선택적 프록시 비용(월 약 1달러) 발생
- 자격증명은 로컬 `~/.agent-reach/config.yaml`에 파일권한 600으로 저장, 외부 업로드 없음 — 6장 최소권한 원칙에 부합하는 설계

### 플랫폼 접점
- 원래 목표(외부정보 수집·분석)에 소셜 플랫폼까지 확장
- 25장/28장 콘텐츠 생성·수익화의 **트렌드 리서치 단계**에 유용 — 콘텐츠 기획 전 소셜 반응 파악

### 우려 사항 — 다른 채택 도구와 다른 종류의 리스크
1. **계정 정지 위험**: 쿠키 로그인 플랫폼(Twitter, 샤오홍슈 등)은 스크립트 호출이 탐지되어 계정 제한·정지될 위험이 있어 프로젝트 자체가 전용 부계정 사용을 명시적으로 권장 — **28장의 실제 수익화 채널 계정과 절대 혼용 금지**
2. **플랫폼 ToS 위반 소지**: 쿠키 기반 스크래핑은 기술적 가능과 약관상 안전이 별개 문제
3. **수집 콘텐츠는 "외부 데이터"로 취급**: Reddit/Twitter에서 가져온 텍스트는 6장·7장 프롬프트 인젝션 방어 대상 그대로 적용 — 소셜 콘텐츠는 조작된 텍스트 섞일 가능성 특히 높음

### 채택 방식
- 용도를 **"리서치 전용"으로 제한**: `skills/content-generation/trend-research/`로 분리, 콘텐츠 기획 단계에서만 사용하고 자동 게시 파이프라인과 분리
- 쿠키 인증 필요 플랫폼(Twitter, 샤오홍슈)은 **전용 리서치용 부계정** 사용 — 실제 수익화 채널 계정과 분리
- YouTube 자막·GitHub·RSS 등 **쿠키 불필요 채널부터 우선 활용** (계정 정지 리스크 없음)
- 신규 의존성이므로 **6장 Bumblebee로 설치 전후 스캔** 권장

---

## 30. Khoj 병행 평가 — 8장/22장 대안 구현체

AGPL-3.0 오픈소스 "AI 세컨드 브레인". 3만4천+ GitHub 스타, Y Combinator(W24) 지원. 비공개 문서 시맨틱 검색 + Ollama/클라우드 LLM 대화를 결합, 이미지·PDF·마크다운·org-mode·Notion·GitHub 저장소 등 다양한 형식 지원, **Obsidian 플러그인 기본 제공**, 커스텀 에이전트·반복 리서치 자동화·개인화 뉴스레터 기능 포함.

### 판단이 복잡한 이유
지금까지 "좁은 유틸리티(채택)" vs "범용 오케스트레이션(신중)" 이분법으로 판단해왔으나, Khoj는 **8장(NotebookLM 기능)+22장(옵시디언 연동)과 거의 정확히 같은 스코프를 가진 성숙한 수직 솔루션**이라 중간 지점에 위치.

### 긍정 요소
- 스코프가 지식관리에 국한되어 전체 플랫폼 오케스트레이션을 뺏으려 하지 않음(Dify/Langflow보다 위험도 낮음)
- Ollama·Qwen 네이티브 지원, **Obsidian 플러그인이 이미 완성** — 22장에서 직접 구현하려던 `scan_vault.py`/`parse_links.py` 작업 상당 부분 대체 가능
- 2023년부터 운영된 성숙한 프로젝트, YC 백업 — OpenClaw/OpenWorker보다 검증 기간 김

### 우려 요소
1. **AGPL-3.0 라이선스 — 27/28장 수익화 계획과 충돌 가능성**: 수정 후 네트워크 서비스로 운영 시 수정 소스 공개 의무 발생. 1단계 개인 사용은 무관하나 **3단계에서 이를 기반으로 유료 서비스화 시 법적 검토 필요**
2. **스택이 무거움**: Postgres+pgvector, Django+Gunicorn+Next.js, 코드 실행 샌드박스(Terrarium), SearxNG까지 포함된 다중 서비스 구성 — 7장의 "SQLite+LanceDB 경량 구조"보다 훨씬 무거워 32GB 예산에 추가 부담
3. **자체 에이전트/자동화 시스템 보유**: 스케줄링·자동화·에이전트 생성 기능이 21/23/24장의 "오케스트레이션 주도권 유지" 원칙과 부분 충돌 가능

### 권장 방향: 경쟁 트랙 병행 평가
"채택 또는 신중"으로 단정하기보다 8장+22장의 대안 구현체로 병행 검토:
- 기존 커스텀 NotebookLM/옵시디언 스킬과 별개로 Khoj를 Docker로 가볍게 띄워 **직접 비교 테스트** (Obsidian 연동이 실제로 시간을 얼마나 아껴주는지)
- 비교 후 결정 기준:
  - **채택**: Khoj가 확실히 낫고, 3단계 수익화 계획에 이 기능을 포함시키지 않을 경우 → 커스텀 개발 중단하고 전환
  - **커스텀 유지**: 3단계에서 이 기능을 수익화에 포함시킬 계획이거나 경량 구조를 유지하고 싶은 경우 → Khoj는 UI/기능 참고용으로만 활용

---

## 31. Hermes 평가 — 오케스트레이션 주도권 관점 최종 판단

### 이름 구분 (혼동 주의)
"Hermes"는 3가지를 가리키는 중의적 이름:
- **Hermes (LLM 계열)**: Nous Research의 파인튜닝 오픈웨이트 모델(Hermes 2/3/4) — 강한 지시이행·도구호출, 낮은 거부율
- **Hermes Agent**: 자가개선형(self-improving) 에이전트 프레임워크 — 플래너·도구·스킬·메모리·게이트웨이 포함, MIT 라이선스
- **Hermes Desktop**: Hermes Agent의 GUI 프론트엔드(2026.6 공개)

### Hermes Agent 개요
21만4천+ GitHub 스타, 4만 포크, $75M+ 신규 투자로 $1.5B 가치평가 추진 중(Nous Research, 2023년 설립). 모델 애그노스틱(300+ 모델, Qwen 포함), 지속 메모리(FTS5 전문검색), 스킬 시스템, MCP 카탈로그, 메신저 연동(Telegram/iMessage/WeChat), 샌드박스 실행 백엔드. "다른 에이전트는 하네스를 직접 손으로 짜야 하지만 Hermes는 5개 층을 전부 자동화"한다는 것이 핵심 셀링포인트.

### 도입 시 장단점 비교
| | 장점 | 단점 |
|---|---|---|
| 개발 속도 | 라우팅·메모리·스킬·게이트웨이 이미 구현, 4장(메신저 연동) 상당부분 대체 가능 | 9장에서 손수 설계한 하네스와 구조적으로 중복 |
| 모델 유연성 | 300+ 모델 지원, 실험적 모델 스위칭 쉬움 | 2장의 "9B 상시+30B 온디맨드" 최적화 로직을 존중하지 않을 위험 |
| 학습 자료 | 63페이지 하네스 엔지니어링 핸드북 — 9장 보완 자료로 가치 있음 | 원래 목표("하네스를 직접 깎으며 배우기")와 정면 충돌 |
| 라이선스 | MIT — 27/28장 수익화와 충돌 없음(Khoj의 AGPL과 다름) | "자가개선" 브랜딩이 11장의 자율성 제한 원칙과 충돌 |
| 안정성 신호 | 2주 200+ PR 병합 등 매우 활발한 개발 | 같은 이유로 연동 지점이 다음 업데이트에서 깨질 위험 상존 |

### 성능 우려에 대한 정정 — Ollama의 MLX 네이티브 통합
- Ollama 0.19(2026.3)부터 **MLX가 Apple Silicon 기본 백엔드로 통합**됨 — 별도 설치가 아니라 Ollama 자체의 표준 동작
- 사용자 실측치(9B=38 tok/s, 30B=55 tok/s, GPU:CPU=5:95)는 **M1 Max가 M5의 전용 GPU Neural Accelerator를 갖고 있지 않아 발생하는 세대 차이로 설명 가능** — 튜닝 문제가 아닐 가능성 높음, 재조정 불필요
- MLX는 GPU/CPU를 통합 메모리 풀로 취급해 전통적 "GPU 사용률 %" 지표가 왜곡되어 보일 수 있음 — 5:95 비율 자체에 과도하게 반응할 필요 없음
- **MLX가 Ollama에 내장된 이상, Hermes를 포함한 어떤 도구든 표준 Ollama API만 호출하면 자동으로 MLX 가속 혜택을 받음** — "Hermes가 MLX를 우회할 것"이라는 초기 우려는 해소됨. 단, 이는 성능 측면 우려만 해소하는 것이며 아래 최종 결론에는 영향 없음

### 최종 결론
**메인 플랫폼 오케스트레이션에는 도입하지 않음 — 기존 커스텀 구조(FastAPI+skill 라우터+9장 하네스) 유지.** 성능 우려는 해소되었으나, 하네스 중복·자율성 원칙 충돌·학습 목적 훼손이라는 핵심 이유는 그대로 유효하며, 3장의 "인프라는 빌리고, 로직은 짓는다" 원칙과 일치하는 결정.

**단, 구조/개념은 참조 가치가 확실함**:
- 63페이지 하네스 엔지니어링 핸드북을 9장(하네스) 보완 학습자료로 활용
- Hermes Desktop을 AionUi와 동일한 방식으로 개인 비교·학습용 보조 도구로만 병행 가능
- Hermes LLM 계열(모델 자체)은 도구호출이 강하고 거부율이 낮다는 특성이 있어, 메인 스택(Qwen)은 유지하되 특정 제약 없는 창작 작업에 한해 Ollama에서 실험적 시도는 고려 가능

---

## 32. mattpocock/skills — 스킬 라이브러리 참조 (적극 채택)

TypeScript 교육자 Matt Pocock이 자신의 `.claude/skills/` 폴더를 그대로 공개한 저장소. 4만8천~7만5천대 GitHub 스타, 트렌딩 2위 6일 연속. 각 스킬은 YAML 프론트매터(이름·설명)를 가진 단일 SKILL.md + 필요시 참조 스크립트로 구성, TDD·이슈 트리아지·수직분할(vertical-slicing) 등 엔지니어링 경험을 에이전트 호출 가능한 스킬로 쪼개는 방법론을 실전 예시로 보여줌.

### 채택 판단 — 지금까지 중 가장 리스크 낮음
프레임워크·오케스트레이션 레이어·상시 프로세스가 아니라 **순수 마크다운 파일 모음** — 3장 원칙의 "인프라"보다도 가벼운 층이라 오케스트레이션 주도권 문제 자체가 발생하지 않음.

### 적용 방법
1. **스킬 작성법 참고자료**: "좋은 SKILL.md는 어떻게 생겼는가"를 실전 사례로 확인 — 9장(하네스) SKILL.md 형식을 다듬을 때 참고
2. **범용 개발 방법론 스킬 직접 이식**: TDD 루프, 스펙 작성(spec→ticket 분할), 이슈 트리아지, 디버깅 방법론은 언어 독립적 메타 패턴 — `skills/coding/`에 `tdd-loop/`, `spec-writing/`, `triage/` 등으로 편입, 17장 B(코딩 에이전트) 품질 향상에 직결
3. **TypeScript 특화 스킬은 참고만**: Python/FastAPI 스택엔 직접 적용 안 되지만 "어떤 기준으로 스킬을 쪼갰는지"의 사고방식은 학습 가치 있음

### 주의점
- 콘텐츠라도 6장 원칙대로 가져오기 전 내용 확인, 참조 스크립트가 있으면 최소 검증
- 라이선스 확인 후 인용 표시하며 가져오기

---

## 33. VoltAgent 평가 — 관측성 개념만 참조

TypeScript 기반 오픈소스 AI 에이전트 프레임워크, MIT 라이선스, 메인 저장소 약 1만 스타. 관측성(observability) 중심 설계 — n8n 스타일 시각적 디버깅 콘솔(VoltOps)이 핵심 차별점. 메모리·RAG·가드레일·도구·MCP·음성·워크플로 엔진·감독자-하위에이전트 오케스트레이션까지 포괄하는 풀스택 프레임워크.

### 평가: 신중 접근 (Dify/Langflow/Hermes와 동일 원칙) + 언어 장벽

**오케스트레이션 중복** — 메모리·RAG·가드레일·워크플로까지 전부 포함해 9장(하네스)·2장(메모리 최적화)·6장(가드레일)과 정확히 같은 역할을 하려 함. 감독자-하위에이전트 구조는 26장 "코드 조직은 스킬 폴더 계층화로, 오케스트레이션은 통합 유지" 원칙과 충돌.

**추가 장벽: 언어 불일치** — 백엔드는 FastAPI(Python)인데 VoltAgent는 TypeScript/Node.js 기반이라, 통합하려면 별도 Node 프로세스 병행 또는 백엔드 일부 재작성 필요 — 지금까지 평가한 도구들보다 실질적 통합 비용이 큼.

### 유일한 참고 가치 — 관측성(Observability) 개념
VoltOps 같은 **시각적 에이전트 실행 디버깅 콘솔**은 문서에 아직 없는 빈 곳. 6장 8번(감사 체계)이 "로그화"까지만 다루고, 로그를 보기 쉽게 보여주는 대시보드는 미설계 — 향후 FastAPI 백엔드에 **경량 로컬 관측성 대시보드**(스킬 실행 흐름·소요시간·실패지점 시각화)를 추가할 때 VoltOps를 참고 사례로 활용.

### 결론
메인 오케스트레이션에는 도입하지 않음 — 오케스트레이션 주도권 유지 원칙에 더해 TypeScript/Python 스택 불일치까지 겹쳐 통합 이유가 더 약함. "관측성 대시보드" 아이디어만 6장 감사 체계의 향후 개선 과제로 채택.

---

## 34. 참고 자료 디렉토리 — 탐색 프로토콜

지금까지 평가해온 개별 도구(OfficeCLI, Voicebox, Supertonic, Khoj, Hermes 등)들의 "입구" 역할을 하는 큐레이션 목록 저장소들. 실행 코드가 아니라 색인이므로 오케스트레이션 리스크 자체가 없음 — 개별 도구 평가와 달리 "채택 여부"가 아니라 "활용 방식"이 핵심.

### 목록별 성격과 활용처
| 저장소 | 성격 | 활용처 |
|---|---|---|
| free-for-dev | 개발자용 무료 티어 서비스 모음 | 25장/28장 외부 API(Pexels 등) 무료 대안 탐색 |
| public-apis | 공개 무료 API 대규모 목록 | 17장 H(데이터분석), 28장(콘텐츠 소재) 발굴 |
| awesome-selfhosted | 셀프호스팅 소프트웨어 총망라 | 17장 실험 카탈로그 확장 시 1차 탐색 |
| awesome-claude-code | Claude Code 활용법·워크플로 모음 | 12장(Claude 역할), 9장(하네스) 실전 팁 |
| anthropics/skills | Anthropic 공식 SKILL.md 예제 | 32장(mattpocock)과 동일 성격 — 공식 레퍼런스라 가장 신뢰도 높은 SKILL.md 작성 참고 |
| awesome-mcp-servers | MCP 서버 대규모 색인 | 4장·12장 MCP 연동 확장 시 도구 탐색 |
| awesome-llm-apps | LLM 앱 레퍼런스 구현체 모음 | 8장(NotebookLM), 17장 각 실험의 구현 아이디어 |
| system-prompts-and-models-of-ai-tools | 상용 도구(Cursor·Devin·v0 등) 시스템 프롬프트 모음(일부 유출본) | 9장 하네스 설계 시 구조 학습 — **문구 그대로 복사 지양, IP 이슈 소지** |
| awesome-codex-skills | OpenAI Codex CLI용 스킬 모음 | mattpocock류와 동일 — 범용 개발 스킬 이식 후보 |
| awesome (sindresorhus) | 모든 awesome 리스트의 뿌리 목록 | 새 카테고리 필요 시 최초 진입점 |

### 활용 원칙
1. **분야별 1차 탐색 지점 매핑**: 코딩/스킬 작성→anthropics/skills·mattpocock류·awesome-codex-skills / 외부 API·데이터→free-for-dev·public-apis / 자가호스팅 후보→awesome-selfhosted / MCP 도구→awesome-mcp-servers / 프롬프트·하네스 설계→awesome-claude-code·system-prompts-and-models-of-ai-tools
2. **여기서 찾은 모든 것은 기존 검증 절차를 동일하게 통과**: 목록에 있다고 자동 신뢰 금지 — 6장 Bumblebee 스캔, 21/23/24/30/31/33장의 "오케스트레이션 주도권 유지" 판단, 27/28장 수익화 계획과의 라이선스 충돌 여부 확인
3. **system-prompts-and-models-of-ai-tools 특별 주의**: 상용 제품 유출 프롬프트 포함 — 구조·패턴 학습 용도로만, 문구 그대로 복사 지양 (원저작물 지적재산권 이슈 소지)
4. **정기 점검 루틴**: 16장 온디맨드 시작 루틴에 월 1회 정도 이 목록들을 훑어보는 습관 추가 권장

---

## 35. CapCutAPI / capcut-mate 평가 — 25장 영상 생성 보완 (조건부 채택)

### 공식 CapCut — 자동화 불가
CapCut 데스크톱은 공식 자동화 API·CLI·스크립팅 훅이 전혀 없음. 공식 "AI API"는 배경제거·이미지 업스케일·텍스트→영상·템플릿 검색 정도로 범위가 좁고, 서버사이드 렌더링 엔드포인트는 미제공.

### 커뮤니티 오픈소스 래퍼 (CapCutAPI, capcut-mate)
드래프트 파일(.dfd)을 생성해 CapCut이 열 수 있게 만드는 리버스엔지니어링 방식 프로젝트. **Python+FastAPI 기반**, HTTP API와 MCP 프로토콜 양쪽 지원(`add_video`/`add_audio`/`add_subtitle`/`add_effect` 등). 생성된 드래프트를 CapCut drafts 디렉터리에 복사해 열면 CapCut의 실제 렌더링 엔진과 템플릿 스타일 그대로 결과물 산출.

### 평가
**긍정 요소**
- 스택이 Python/FastAPI로 정확히 일치 — 지금까지 평가한 도구 중 가장 마찰 없는 통합(VoltAgent의 TS 불일치 문제 없음)
- MCP 지원 — 12장 Claude 연동과 자연스럽게 연결
- **25장 MoneyPrinterTurbo의 좋은 보완재**: MoneyPrinterTurbo는 표준적인 스톡영상+TTS 조합, CapCutAPI는 틱톡/쇼츠에서 실제 유행하는 CapCut 스타일 템플릿·자막·효과 활용 가능 — 트렌드 적합성 높음

**우려 요소**
1. **수동 export 단계 불가피** — 서버사이드 렌더링 API가 없어 사람이 직접 CapCut 앱에서 내보내기를 눌러야 완성. 완전 자동화 불가능
2. **리버스엔지니어링 기반의 취약성** — 비공식 드래프트 포맷 리버스엔지니어링이라 CapCut 업데이트마다 깨질 위험 상존
3. **약관 회색지대** — 29장(Agent Reach)과 같은 종류의 ToS 리스크

**관점 전환**: 6장 "external 등급은 항상 사람 승인 필수" 원칙과 맞춰보면, 이 수동 export 단계가 오히려 자연스러운 승인 게이트 역할 — 완전 자동 발행 불가가 단점이자 28장 "검수 없는 자동 발행 금지" 원칙을 강제하는 안전장치이기도 함.

### 결론 — 조건부 채택
```
skills/content-generation/capcut-draft/
  SKILL.md
  capcutapi_client.py   # 드래프트 생성/자막/효과 자동화
```
25장 영상 생성에 MoneyPrinterTurbo와 나란히 추가 — "표준 스타일"은 MoneyPrinterTurbo, "트렌드 스타일"은 CapCutAPI로 이원화. 버전 호환성 문제 발생 시 즉시 격리·재검토. 최종 export는 항상 사람이 CapCut 앱에서 직접 수행.

---

## 36. ponytail 평가 — 규칙 주입형 코딩 스킬 (적극 채택)

### 개요
AI 코딩 에이전트가 "가장 게으른, 그러나 작동하는" 해법을 내도록 만드는 규칙 세트. JavaScript, MIT 라이선스, 9만6천+ 스타. 실행 프레임워크가 아니라 **매 턴 주입되는 SKILL.md+훅** — 32장(mattpocock/skills)과 같은 저리스크 부류.

- 규칙 순서: 이 작업이 정말 필요한가(YAGNI) → 코드베이스에 이미 있는가 → 표준 라이브러리로 되는가 → 네이티브 플랫폼 기능으로 되는가
- lite/full/ultra 강도 조절, 서브에이전트로도 규칙 전파
- Claude Code, Cursor, Windsurf, Cline, Copilot CLI, Codex, Gemini/Antigravity CLI 등 지원
- 자체 벤치마크(저자가 과장 가능성을 스스로 인정하고 재검증): 에이전틱 실사용 기준 60~94% 코드 절감, 이미 최소화된 코드는 무변화

### 당신 플랫폼 적용 — 두 갈래
**1. Claude Code에 직접 설치** (12장 직결): 공식 지원 호스트이므로 11장 "플랫폼 유지보수 시 Claude Code 호출" 워크플로에 바로 적용 — 작은 PR은 사람 리뷰가 쉽고(11장 원칙과 시너지), 불필요한 의존성 추가가 줄어 6장 공급망 리스크도 감소

**2. 규칙 텍스트를 자체 코딩 스킬에 이식** (17장 B 직결): 당신의 FastAPI+Qwen 하네스는 ponytail의 훅 메커니즘을 지원하는 표준 호스트가 아니므로 플러그인 설치는 불가하지만, SKILL.md 규칙 텍스트 자체는 이식 가능 — `skills/coding/`에 옮겨 코딩 스킬 시스템 프롬프트에 포함

### 당신 환경에 특히 유리한 이유 — 2장 메모리 최적화와의 시너지
32GB 제약에서 "코드를 적게 쓰게 만드는 규칙"은 품질 개선을 넘어 실질적 자원 절약: 짧은 코드 = 적은 토큰 = 빠른 생성 = 좁은 컨텍스트 예산(9B/30B)에 더 잘 들어맞음 — 지금까지 평가한 도구 중 하드웨어 제약과 직접 맞아떨어지는 유일한 사례.

### 결론
```
skills/coding/lazy-dev-ruleset/
  SKILL.md   # ponytail 규칙 텍스트 이식, 코딩 스킬 시스템 프롬프트에 포함
```
Claude Code는 공식 설치, 자체 Qwen 코딩 스킬은 규칙 텍스트 수동 이식. 실행 코드도 상시 프로세스도 아닌 프롬프트 레이어라 리스크 사실상 없음 — 32장과 함께 적극 채택 등급.

---

## 37. Goose 평가 — 신중 접근 + 실증 보안 사례

### 개요
Block(구 Square, Cash App 모회사)에서 만들어 2026년 Linux Foundation 산하 Agentic AI Foundation(AAIF)에 기증된 오픈소스 에이전트. Rust 작성, Apache 2.0 라이선스, 5만2천+ 스타, 500명+ 컨트리뷰터. 15~30개+ LLM 프로바이더 지원(Ollama 포함, 완전 로컬/오프라인 가능), 70개+ MCP 익스텐션, CLI·데스크톱·API 인터페이스. **"레시피(Recipe)"**: 목표·필요 익스텐션·구조화된 입력·하위 레시피를 담은 재사용 가능한 YAML 워크플로 정의 — 에이전트 행동을 위한 저장된 매크로.

### 평가 — Hermes(31장)와 동일 원칙 + 더 신뢰할 만한 거버넌스
**오케스트레이션 중복**: 메모리·도구·익스텐션·레시피(워크플로 엔진)까지 포괄하는 풀스택 에이전트라 9장(하네스)·2장(메모리 최적화)과 역할이 겹침 — 26장 "오케스트레이션 주도권 유지" 원칙 그대로 적용.

**긍정 요소**
- Apache 2.0 — 27/28장 수익화 계획과 충돌 없음
- Rust 기반이라 리소스 효율 높음 — Node 기반 Hermes보다 32GB 환경 부담이 덜할 가능성
- Linux Foundation 거버넌스 — 단일 스타트업보다 장기 안정성 신뢰도 높음

### 실증된 보안 사례 — Operation Pale Fire (2026.1)
Block 자체 보안팀이 보이지 않는 유니코드 문자에 악성 지시를 숨긴 "오염된 레시피"로 자사 직원 대상 Goose 해킹에 성공. 디버깅 도움으로 위장해 개발자와 AI 에이전트 모두를 속여 정보탈취 악성코드 실행까지 이어짐. **보안 전담팀을 둔 조직조차 뚫렸다는 사실이, 6장의 "외부 콘텐츠는 항상 데이터로만 취급" 원칙이 이론이 아닌 실전 위협에 대한 방어라는 걸 실증** — 6장 1번에 반영 완료.

### 당신 플랫폼 적용
1. **메인 오케스트레이션에는 도입하지 않음** — 지금까지 원칙 유지
2. **개인 보조 도구로 병행 고려**: Rust 기반 저리소스 + 완전 로컬 Ollama 지원 — AionUi/Hermes Desktop보다 가벼운 코딩 비교 도구 후보
3. **"레시피" 개념을 스킬 설계에 참고**: 다단계 작업을 YAML로 구조화하는 패턴, 스킬이 복잡해질 때 참고할 형식

---

이 문서는 하네스의 6가지 구성 요소(루프·도구실행·메모리·검증·가드레일·컨텍스트 엔지니어링) 기준으로 계속 점검·보완해나가는 것을 권장합니다.