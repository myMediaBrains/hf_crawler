# 구현 로그 인덱스

이 문서는 `requirements_refactoring.md`(마스터플랜)를 실제로 구현해나가는 과정을
단계별로 추적하는 목차입니다. 새 구현 단계를 시작할 때마다 아래 표에 한 줄을
추가하고, `PROJECT_STATUS_TEMPLATE.md`를 복사해서 `PROJECT_STATUS_NN_주제.md`로
새 로그를 만드세요.

**원칙**: 마스터플랜(`requirements_refactoring.md`)은 "무엇을, 왜" 결정했는지를 담는
정적인 문서입니다. 이 인덱스와 개별 `PROJECT_STATUS_NN_*.md`들은 "어떻게 만들었고,
어떤 버그를 겪었고, 지금 어디까지 됐는지"를 담는 동적인 기록입니다. 새 대화를 시작할 때는
이 인덱스 + 해당 단계 파일만 첨부하면 되고, 마스터플랜 전체를 매번 다시 올릴 필요는
없습니다(단계 파일 안에 관련 장 번호를 명시해뒀으므로).

---

## 진행 현황

**상태 표기 규칙**: `구현 중` → `구현 완료 (검증 대기)` → `검증 완료 ✅` 순서로만 이동합니다.
**`검증 완료 ✅`가 아닌 단계 위에는 다음 번호의 새 구현을 시작하지 않습니다.**

| 번호 | 파일 | 주제 | 마스터플랜 연결 장 | 상태 | 기간 |
|---|---|---|---|---|---|
| 01 | `PROJECT_STATUS_01_hf_crawler_foundation.md` | hf_crawler 기반 구축 — 수집/정제/번역/편집 플랫폼 | Part1-4(Skills), Part1-7(전처리 파이프라인), Part1-9(저장/백업) | 구현 완료 (검증 대기) | 2026-08-02 ~ 08-07 |

*(다음 단계가 시작되면 여기 새 행을 추가합니다. 번호는 항상 구현에 착수한 순서를 그대로 따릅니다 — 나중에 순서가 바뀌어도 번호를 재배열하지 않고, 새 번호를 이어서 부여합니다.)*

---

## 단계 전환 규칙 (검증 게이트)

1. 한 단계의 구현이 끝나면, 해당 `PROJECT_STATUS_NN_*.md`의 **"8. 검증 체크리스트"** 항목을
   실제로 하나씩 확인합니다 (기능을 직접 실행해보고 결과를 확인).
2. 전부 확인되면 그 문서의 상태를 `검증 완료 ✅`로 바꾸고, 이 인덱스 표의 상태도 같이 갱신합니다.
3. **검증 완료 전에는 다음 번호(N+1)의 `PROJECT_STATUS_NN_*.md`를 새로 만들지 않습니다.**
   검증 중 발견된 문제는 같은 단계 문서 안에서 수정 기록으로 남기고, 문제가 다 해결된
   뒤에야 다음 단계로 넘어갑니다.
4. 예외: 검증 도중 "이 문제는 다음 단계에서 다뤄도 무방하다"고 명시적으로 판단한 경우,
   그 사유를 해당 단계 문서의 "6. 남은 작업"에 남기고 넘어갈 수 있습니다 (조용히 넘어가지
   않고, 반드시 기록으로 남기는 게 핵심입니다).

---

## 새 구현 단계를 시작하는 방법

1. `PROJECT_STATUS_TEMPLATE.md`를 복사해서 `PROJECT_STATUS_NN_주제slug.md`로 이름 변경
   (`NN`은 2자리 숫자, 예: `02`, `03` — 정렬이 흐트러지지 않도록 항상 0을 채움)
2. 문서 최상단에 **관련 마스터플랜 장 번호**를 반드시 명시 (`requirements_refactoring.md`의
   어느 장을 구현하는 것인지)
3. 작업이 끝나면(혹은 대화가 길어져 다음 대화로 넘어갈 때) 이 인덱스 표에 한 줄 추가
4. 새 대화를 시작할 때는 **이 인덱스 파일 + 이어서 할 단계의 `PROJECT_STATUS_NN_*.md`**만
   첨부하면 충분합니다. 이전 단계 파일들은 "파일 인벤토리"에 남아있는 기록만으로도
   대부분 충분하지만, 그 단계의 코드를 직접 다시 건드려야 하면 해당 파일도 같이 첨부하세요.

## 명명 규칙

```
PROJECT_STATUS_01_hf_crawler_foundation.md
PROJECT_STATUS_02_<다음-주제-영문-슬러그>.md
PROJECT_STATUS_03_...
```

주제 slug는 마스터플랜의 스킬 폴더명과 맞추는 걸 권장합니다 (예: 다음 단계가
`skills/content-generation/sns-publish/`를 구현하는 거라면 `PROJECT_STATUS_02_sns_publish.md`).

**번호는 구현에 실제로 착수한 순서를 그대로 반영합니다** — "몇 번째로 만들어졌는가"를
파일명만 보고 바로 알 수 있게 하기 위함입니다. 이미 번호가 매겨진 파일의 번호는 절대
재사용·재배열하지 않습니다 (중간에 계획이 취소돼도 그 번호는 비워두고 다음 번호로
넘어갑니다 — 예: 03이 취소됐다면 다음은 05가 아니라 04이지만, 03 파일 자체는 "취소됨"
상태로 남겨 이력을 보존).